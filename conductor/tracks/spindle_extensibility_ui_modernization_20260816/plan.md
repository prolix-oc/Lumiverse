# Implementation Plan: Spindle Extensibility V2, Universal UI Mounts, Toolbar Dynamics, Connection Picker Modernization, and Settings/Editing Modernization

## Operating Contract

- Canonical implementation plan: G:/AI/All lumiverse repos/Lumiverse/conductor/tracks/spindle_extensibility_ui_modernization_20260816/plan.md.
- User artifact mirror: C:/Users/Sovex666/.gemini/antigravity/brain/8c253402-1448-48a5-94c3-fe5205e4f9c3/implementation_plan.md.
- Host branch: staging in G:/AI/All lumiverse repos/Lumiverse.
- Public SDK checkout: G:/AI/All lumiverse repos/lumiverse-spindle-types.
- First-party consumer: G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite.
- The existing host bridges and loader cleanup are the baseline. Tasks promote and extend those contracts; they do not create parallel settings, state, domain, surface, or teardown systems.
- Every phase has the blocking executable command, named test cases, and measurable pass condition in the Mandatory Executable Verification Matrix. No phase is accepted by a generic reference to workflow.md.
- Extension code remains free of raw document-wide MutationObservers and direct core DOM scraping. Host code owns centralized observation, sanitization, ownership stamping, replay, and cleanup.

## Traceability IDs

- A: public SDK non-breaking additivity and package compatibility.
- B: 58 standardized mounts, 11 component overrides, decorators, settings sections, message actions, and clean teardown.
- C: permissioned dynamic embedding, TTS, STT, and sidecar registries.
- D: toolbar geometry, stable docking, drag behavior, icons, eight docker actions, and customizable action bar.
- E: bootstrap provider persistence, grid/list, cache, search, and refresh.
- F: embedding fallback, Memory Cortex secondary fallback, and Edit and Send history semantics.

## Phase 0: Baseline, Contract Decisions, and Release Chain

### Task 0.1: Create the canonical implementation map

- [ ] Record the exact source files and current behavior before edits in review/evidence-ledger.md.
- [ ] Use these existing host contracts as the source for SDK promotion:
  - frontend/src/lib/spindle/settings-bridge.ts
  - frontend/src/lib/spindle/settings-tab-bridge.ts
  - frontend/src/lib/spindle/state-selectors.ts
  - frontend/src/lib/spindle/frontend-domain-api.ts
  - frontend/src/lib/spindle/frontend-context.ts
  - frontend/src/lib/spindle/host-surfaces.ts
  - frontend/src/lib/spindle/components-helper.tsx
  - frontend/src/lib/spindle/loader.ts
- [ ] Correct path mapping before implementation:
  - Current LandingPageShell contract is implemented by frontend/src/components/landing/LandingPage.tsx.
  - CharacterCard is frontend/src/components/panels/character-browser/CharacterCard.tsx.
  - WorldBookEntryEditor is frontend/src/components/shared/WorldBookEntryEditor.tsx.
  - Preset tabs and toolbar live in frontend/src/components/panels/LoomBuilder.tsx.
  - Persistent geometry hook is frontend/src/hooks/usePersistentRect.ts.
  - TTS uses src/tts/registry.ts and src/services/tts.service.ts.
  - STT uses src/services/stt-connections.service.ts and src/services/stt.service.ts.
  - Sidecar execution uses src/services/memory-cortex/sidecar-adapter.ts, retry orchestration uses src/services/memory-cortex/index.ts, and configuration uses src/services/memory-cortex/config.ts.
- [ ] Record all existing baseline failures and unrelated generated frontend/dist changes; do not include them in the plan revision commit.

### Task 0.2: Freeze the additive compatibility policy

- [ ] Keep all existing public SDK exports and the four legacy SpindleMountPoint literals source-compatible.
- [ ] Add new frontend capabilities as optional capability-gated members on the existing context, or as a new SpindleFrontendContextV2 interface that extends the existing context. Do not add required properties that break legacy mocks or host implementations.
- [ ] Add a named V2 host capability and require extensions to check it before using settings, state, geometry, surfaces, decorators, overrides, or provider registries.
- [ ] Preserve existing domain signatures while adding the missing methods. In particular, do not remove existing token methods such as countChat or change world-book list/entries semantics.
- [ ] Define idempotent update, destroy, on, and disposer behavior for every new handle.
- [ ] Add assignability assertions for legacy context, V2 context, host-local context, suite context, and every promoted API.
- [ ] Release `lumiverse-spindle-types` additively as `0.6.16`; make zero breaking changes to existing public types and retain a `0.6.15` consumer compatibility fixture.

### Task 0.3: Align the SDK package and first-party consumers

- [ ] In `G:/AI/All lumiverse repos/lumiverse-spindle-types/package.json`, set `version` to `0.6.16`, set `types` to `./dist/index.d.ts`, set `exports["."].types` to `./dist/index.d.ts` and `exports["."].default` to `./dist/index.js`, add `files: ["dist"]`, pin the exact TypeScript version in `devDependencies`, and add exactly `build: tsc -p tsconfig.json` and `build:consumer: tsc -p tsconfig.consumer.json --noEmit && node test/scripts/verify-packed-consumer.mjs` scripts. Commit `package-lock.json` and require `npm ci` before these commands; `npm run build:consumer` is the only named packed-consumer verification command.
- [ ] Keep `G:/AI/All lumiverse repos/lumiverse-spindle-types/tsconfig.json` as the declaration producer with `rootDir: ./src` and `outDir: ./dist`; require `npm run build` to produce `dist/index.js` and `dist/index.d.ts` plus matching JS/declaration files for every exported source module. Keep `tsconfig.consumer.json` as a no-emit fixture compiler with `noEmit: true`, no `outDir`, and explicit includes for `test/package-exports-consumer.ts`, `test/legacy-0.6.15-consumer.ts`, `test/packed-0.6.16-consumer.ts`, and `test/provider-runtime-union-parity.ts` plus every named compatibility/provider fixture.
- [ ] Add `G:/AI/All lumiverse repos/lumiverse-spindle-types/test/package-exports-consumer.ts`, `test/legacy-0.6.15-consumer.ts`, `test/packed-0.6.16-consumer.ts`, and the canonical `test/scripts/verify-packed-consumer.mjs`. `package-exports-consumer.ts` imports only `lumiverse-spindle-types` through the package export; `legacy-0.6.15-consumer.ts` compiles only the 0.6.15 public types/mounts; and the packed source imports the package name only. Execute the local SDK TypeScript fixtures with `tsc -p tsconfig.consumer.json --noEmit` under the pinned toolchain.
- [ ] `test/scripts/verify-packed-consumer.mjs` runs `npm pack --json`, captures its emitted `filename`, resolves that filename to an absolute tarball path, and in `try/finally` creates and cleans `test/fixtures/packed-0.6.16-consumer`. It creates the fixture `package.json`, `tsconfig.json`, and copied `packed-0.6.16-consumer.ts`; installs only the captured absolute tarball with `npm --prefix <fixture> --cache <fixture>/npm-cache install --ignore-scripts --no-package-lock --no-save <absolute-tarball>`; asserts the installed package version is `0.6.16`, its `types` and `exports["."].types` are `./dist/index.d.ts`, `dist/index.d.ts` exists, and package export resolution reaches `node_modules/lumiverse-spindle-types/dist/index.js`; runs the fixture `tsc --noEmit`; and removes the fixture, isolated cache, and emitted tarball in `finally` on success or failure. The fixture has no SDK-source `paths`, symlink, workspace, or parent-directory resolution.
- [ ] Task 0.3 prepares only the SDK version, declaration build, `package-lock.json`, compatibility fixtures, and local packed-distribution proof. It must not publish or change any consumer manifest or lockfile. Before any release action, `npm ci`, `npm run build`, all package-root/declaration checks, the local TypeScript fixtures, and `npm run build:consumer` must pass. A separate local-consumer flow may install the captured absolute tarball into the host/frontend/suite fixture when those consumers need the tarball before publication; that flow does not duplicate the canonical verifier command.
- [ ] Sequence Task 0.3 as: SDK version/package metadata; public export declaration build; package-root and 0.6.15 compatibility fixtures; canonical packed `dist` fixture; then stop for the post-Task-1.3 release gate. Preserve source compatibility and additivity of all existing SDK types and mounts throughout.

### Task 0.4: Establish executable verification commands

- [ ] SDK commands, fixtures, packed-tarball installation, expected declaration outputs, and A pass conditions are the Phase 0 matrix rows.
- [ ] Suite commands and the promoted-API/no-raw-DOM-scraping case are the Phase 3.4 matrix row.
- [ ] Host frontend/backend commands, named files/cases, and B--F pass conditions are the Phase 1--6 matrix rows.
- [ ] Use only `npm run build:consumer` for the canonical packed-consumer proof. If a host/frontend/suite compile needs the local tarball before publication, use a separate named local-consumer verification flow that consumes the absolute tarball emitted by the canonical verifier, with isolated fixture/cache cleanup in `finally`; do not add another ad hoc packed command to the matrix.
- [ ] Add `G:/AI/All lumiverse repos/Lumiverse/scripts/perf/measure-frontend-entry-gzip.mjs`. It must parse `frontend/dist/index.html` (not a Vite manifest, because the current `vite.config` has `manifest: false`), resolve the module `<script type="module" src="...">` asset, reject an absent/ambiguous script or a resolved path outside `frontend/dist`, hash the emitted asset with SHA-256, and compute gzip bytes in memory with the pinned method `node:zlib.gzipSync(Buffer, { level: 9 })` (method version `gzipSync-level-9-v1`, using the exact running `nodeVersion`). It must write no `.gz` or other generated file into `dist` and emit one deterministic compact JSON object with stable key order containing `schemaVersion`, `sourceCommit`, `sourceTreeHash`, `assetRelativePath`, `assetSha256`, `gzipBytes`, `method`, `methodVersion`, and `nodeVersion`.
- [ ] Establish and retain `G:/AI/All lumiverse repos/Lumiverse/conductor/tracks/spindle_extensibility_ui_modernization_20260816/review/baselines/frontend-entry-gzip.json` from a disposable clean detached worktree at the recorded pre-execution base commit. Record the worktree path, base/current source commit, and exact JSON baseline explicitly in `review/evidence-ledger.md`; build and measure only inside that disposable worktree, verify each native command's `$LASTEXITCODE` immediately, and delete the disposable worktree afterward. The baseline JSON must contain `sourceCommit` equal to the measured base commit, `sourceTreeHash` equal to its tree, asset path/SHA, positive `gzipBytes`, `method`, `methodVersion`, and `nodeVersion`. Never measure from or modify the main dirty `frontend/dist`, and never overwrite the baseline during comparison.
- [ ] Compare from `G:/AI/All lumiverse repos/Lumiverse` after `npm --prefix frontend run build:checked` with `node scripts/perf/measure-frontend-entry-gzip.mjs --baseline conductor/tracks/spindle_extensibility_ui_modernization_20260816/review/baselines/frontend-entry-gzip.json`; the script must validate baseline JSON schema, `sourceCommit`/`sourceTreeHash` against the intended comparison source identity, `method`/`methodVersion`/`nodeVersion`, the resolved asset path and asset SHA-256, and `gzipBytes > 0` before calculating `((current.gzipBytes - baseline.gzipBytes) / baseline.gzipBytes) * 100`. It must emit deterministic compact JSON evidence and fail blocking when the delta is greater than `5.00%` (including any metadata/hash/source-identity mismatch). Before and after measurement, capture `git status --porcelain -- frontend/dist` and require empty output; the script must never write generated gzip or other files into `dist`.
- [ ] `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite/tests/suite-public-sdk-boundary.static.test.ts` must contain the named case `rejects extension-owned raw MutationObserver and document-wide core-selector scraping`. It recursively scans extension-owned source outside the explicit host bridge allowlist and fails on `new MutationObserver`, `MutationObserver(`, `document.querySelector`, `document.querySelectorAll`, or selectors targeting core host structure. The Phase 3.4 and Phase 6 suite commands run this named file; its numeric report is `rawMutationObservers=0 documentWideCoreSelectorScrapes=0`.
- [ ] Record command working directory, command text, exit code, and concise result in review/evidence-ledger.md.

## Phase 1: Public SDK and Existing Host Contract Promotion

### Task 1.1: Add the additive SDK contract

- [ ] Update lumiverse-spindle-types/src/components.ts with SpindleHostSurfaceHandle and mountHostSurface(target, surfaceId, props), including idempotent update, destroy, event subscription, and post-destroy behavior.
- [ ] Update lumiverse-spindle-types/src/dom.ts with:
  - SpindleSettingsAPI and the complete core settings API.
  - SpindleSettingsTabHandle and settings tab/section registration handles.
  - Existing domain APIs plus registerEntryDecorator and its disposer.
  - SpindleStateSelectors, SpindleGeometryAPI, and SpindleHostSurfaceAPI.
  - Typed registerComponentOverride, registerDomDecorator, and registerMessageAction.
  - onTeardown and the V2 capability descriptor.
  - The complete 58-value SpindleMountPoint union while preserving legacy values.
- [ ] Make `G:/AI/All lumiverse repos/lumiverse-spindle-types/src/api.ts` the canonical additive provider transport contract. Define JSON-serializable provider descriptor, invocation request, result, and error DTOs plus owner, activation generation, permission scope, correlation ID, and provider kind/ID metadata. Callbacks/functions remain worker-side implementation details and never appear in an SDK DTO, transport envelope, descriptor, request, result, or error.
- [ ] In `api.ts`, add directionally correct SDK unions: `WorkerToHostProviderMessage` contains `provider_register`, `provider_unregister`, and `provider_result`, then `HostToWorkerProviderMessage` contains `provider_invoke`, `provider_abort`, and `provider_changed`; their additive `ProviderRuntimeMessage` union has exactly these six discriminators. Every invoke/result/abort carries one non-empty correlation ID; registration/change messages carry owner, generation, permission scope, kind, and ID as applicable. Re-export these variants from `src/index.ts` and consume them through `src/spindle-api.ts` without replacing any existing SDK union member.
- [ ] Specify in `api.ts` that descriptor JSON is at most 64 KiB, request JSON at most 256 KiB, result/error JSON at most 1 MiB, and every complete envelope at most 1 MiB after UTF-8 serialization; reject non-JSON-safe values, prototype-polluting keys, functions, callbacks, code, and HTML at the receiving boundary. Set the default invocation timeout to 30,000 ms; define the documented provider error DTO for malformed, oversize, permission-revoked, stale-generation, timeout, abort, and unavailable outcomes. A timeout, caller abort, unload, generation rollover, or permission revocation sends exactly one `provider_abort`; hosts suppress every later `provider_result` for that correlation ID.
- [ ] Update `src/spindle-api.ts` with worker-facing registration handles for `registerEmbeddingDriver`, `registerTtsEngine`, `registerSttEngine`, and `registerSidecarEndpoint`; their closures/callbacks stay in `worker-runtime.ts`, while only their data descriptors and invocation/result/error DTOs use the SDK provider unions. Define duplicate `(kind,id)` rejection, owner/generation cleanup, and `provider_changed` publication as additive public behavior.
- [ ] Require `G:/AI/All lumiverse repos/Lumiverse/src/spindle/worker-runtime.ts` to import `WorkerToHostProviderMessage` and `HostToWorkerProviderMessage` from the SDK, extend `RuntimeWorkerToHost` immediately after its `WorkerToHost` member and `RuntimeHostToWorker` immediately after its `HostToWorker` member, create the worker callback map beside the existing handler maps before `const spindleApi`, send provider messages through `post` beside the existing registration methods, and dispatch host provider messages in `handleHostMessage` at line 4061 before the default/non-provider cases. Require `G:/AI/All lumiverse repos/Lumiverse/src/spindle/worker-host.ts` to import the same SDK variants, extend the corresponding `RuntimeWorkerToHost`/`RuntimeHostToWorker` unions at lines 277 and 560, validate provider messages before `handleMessageInScope` at line 1593, and add the six switch cases at line 1601 beside the existing registration/RPC cases. `G:/AI/All lumiverse repos/Lumiverse/src/spindle/runtime-transport.ts` remains transport-only: it accepts `unknown`, applies generic structured-clone/JSON-safe size validation, and must neither declare nor own provider unions.
- [ ] Add SDK compile-time parity fixture `G:/AI/All lumiverse repos/lumiverse-spindle-types/test/provider-runtime-union-parity.ts` with test name `keeps provider runtime discriminators directionally exhaustive`; it asserts all and only the six discriminators are assigned to the correct SDK union. Add runtime round-trip tests `G:/AI/All lumiverse repos/Lumiverse/src/spindle/runtime-transport.provider-rpc.test.ts` — `round trips each SDK provider discriminator without callbacks`, `rejects malformed and oversize provider envelopes`, and `suppresses late provider_result after abort` — plus `src/spindle/worker-host.provider-rpc.test.ts` — `rejects stale generation and revoked permission before dispatch` and `forwards one abort for timeout cancellation and unload`. Run these together with the existing provider registry tests in Phase 3.
- [ ] Define registration schemas with the canonical public uniqueness key `{effectiveScope, installationId, kind, id}`, then `owner`, `generation`, and `recordRevision` are provenance/version fields, not uniqueness fields. Same-owner same-generation identical registration is idempotent; the same key with a different owner is rejected; a newer activation generation atomically replaces only its own stale record. `recordRevision` is monotonic per registry record and is present in storage, register/change/remove events, projections, removal results, and reconnect snapshots; reject out-of-order revisions and stale generations everywhere.
- [ ] Define the exact server-side scope algorithm: `system` is the approved shared system scope; `operator:<approvedOperatorId>` is an approved shared/operator scope; `user:<authenticatedSubject>` is derived from the authenticated consumer/installation owner and is never accepted from worker payloads. A shared/operator worker may publish only to its approved system/operator scope; a user-installed worker may publish only to its server-derived user scope. Bind the authenticated consumer user separately at list/invoke time and filter visibility then. Name `ProviderManager.register`, `list`, `invoke`, `revoke`, `reconnect`, and `disposeGeneration` as the lifecycle API; `ProviderManager` owns persistence, grants, pending calls, revisions, event emission, and generation cleanup.
- [ ] Add named migration `G:/AI/All lumiverse repos/Lumiverse/src/db/migrations/102_spindle_provider_scope.sql` and update `G:/AI/All lumiverse repos/Lumiverse/src/db/baseline.sql`: preserve immutable extension installation identity, add visibility scope to installations, migrate identifier-global grants to scoped permission grants, and add provider records keyed by `{effectiveScope, installationId, kind, id}` with owner, generation, monotonic `recordRevision`, descriptor, status, and timestamps. Migration must backfill existing installations/grants into the server-derived user/system/operator scope without trusting worker user IDs, preserve existing grant decisions, enforce uniqueness, and be idempotent.
- [ ] Specify the executable no-secret worker model for all six provider discriminators. A worker callback has a two-stage host broker: `prepare` returns only a `BrokerRequest` descriptor `{kind,id,method,url,headers,body,bodyEncoding,expectedResponseEncoding,timeoutMs,allowlistKey,correlationId,round}`; the host validates URL/method/headers/body/size/allowlist, resolves the host-held secret, injects authentication, and performs HTTP; `complete` receives only sanitized `BrokerResponse` `{status,headers,body,bodyEncoding,contentType,ok,correlationId,round}` and returns the final `provider_result`. DTOs also define an opaque pending-call binding, cancellation/`AbortSignal`, binary base64/byte-limit handling, deadline, redaction rules, and exactly-once terminal completion. No raw secret, secret reference, authorization header, or secret-bearing error reaches the worker, transport, event, projection, cache, or log. Add successful authenticated custom embedding, TTS, STT, and sidecar broker tests.
- [ ] Update lumiverse-spindle-types/src/index.ts and every relevant public re-export.

### Task 1.2: Promote the already implemented host bridges

- [ ] Align frontend/src/lib/spindle/frontend-context.ts with the SDK using the additive V2 context contract; do not duplicate settings, domain, state, geometry, or surface implementations.
- [ ] Keep settings-bridge.ts and settings-tab-bridge.ts as the implementation for settings get/set/watch and dynamic extension tabs.
- [ ] Keep frontend-domain-api.ts, state-selectors.ts, host-surfaces.ts, and components-helper.tsx as the implementation for their existing APIs.
- [ ] Extend loader.ts context composition and cleanup so every new handle is registered with the existing extension teardown path in frontend-extension-cleanup.ts and loader cleanup.
- [ ] Add world-book entry decorator registration to the existing domain implementation rather than creating a second world-book API.
- [ ] Add host-to-SDK compile-time signature assertions for settings, domains, state, geometry, surfaces, teardown, and component helpers.

### Task 1.3: SDK and host contract tests

- [ ] Add package-root fixtures importing every new SDK export.
- [ ] Add a legacy context fixture proving old structural contexts remain assignable.
- [ ] Add a V2 host fixture proving the current Lumiverse context satisfies the new contract.
- [ ] Add handle lifecycle tests for update, event subscription, idempotent destroy, post-destroy calls, and extension unload.
- [ ] Add mount union parity tests against the canonical 58-value list.
- [ ] Execute the Phase 1 matrix command exactly; its four existing host contract/lifecycle cases and Phase 0 SDK fixtures are the required A/B evidence.

### Phase 1 gate

- [ ] A1, A2, and A3 are green in the evidence ledger.
- [ ] No suite fallback is removed until the aligned packed SDK consumer fixture passes.
- [ ] Post-Task-1.3 release gate: do not publish or alter production consumer manifests/locks until all Phase 1 public V2 exports, SDK-owned provider unions, declaration outputs, package-root and 0.6.15 compatibility fixtures, canonical packed fixture, and the Phase 1 matrix command pass. The captured local tarball may supply every consumer compile before external publication.
- [ ] `npm publish` requires explicit release authority whose stop condition is recorded in a structured authority record, and may target only `https://registry.npmjs.org/`; abort for any other configured or requested registry. Before publishing, run `npm view lumiverse-spindle-types versions --json --registry https://registry.npmjs.org/`, parse the JSON, and assert `0.6.16` is absent; then verify `npm whoami --registry https://registry.npmjs.org/` and `npm owner ls lumiverse-spindle-types --registry https://registry.npmjs.org/` includes that identity and record owner membership, authority owner, scope, timestamp, registry, package, version, and stop condition. Also verify the SDK Git remote is canonical, repository-local author/committer identity matches policy, the SDK tree is clean, and the final `npm pack --json` tarball hash is recorded. The release command is `npm publish --registry https://registry.npmjs.org/ --tag latest --provenance`; if provenance cannot be applied, stop unless the structured authority record explicitly approves that exception. Then verify registry version, tarball URL, integrity, `latest` tag, and provenance before proceeding. Without this authority or any satisfied check, do not publish: local packed verification is sufficient for implementation readiness.
- [ ] Only after approved publication and registry verification may the three consumers update manifests and production locks to `lumiverse-spindle-types@0.6.16`: `G:/AI/All lumiverse repos/Lumiverse/package.json` with `bun.lock`, `G:/AI/All lumiverse repos/Lumiverse/frontend/package.json` with `frontend/bun.lock`, and `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite/package.json` with `spindle-extensions/lumiverse_suite/bun.lock`. Until then, temporary local-tarball overrides are implementation-only and must be removed before any production lockfile is written; commit no `file:`, `link:`, workspace, or local-tarball resolution to a production manifest or lockfile.

## Phase 2: Universal Mounts, Decorators, Overrides, and Settings/Message Anchors

### Task 2.1: Implement the exhaustive 58-anchor ownership matrix

Every row below maps exactly one literal from `spec.md`. `S` means one anchor per mounted host; `R` means one anchor per live keyed instance and replaces the current singleton/exact-one assumption. All `R` rows must be proven by `G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx` (`replays repeated and virtualized mount anchors by stable key`) and the former singleton assertion in `G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/mount-points.test.ts` must be replaced by catalog/cardinality assertions.

| Literal | Full absolute host path | Exact JSX insertion region / anchor | Card. | Virtualization/replay key | Disposer / unmount owner | Exact test file and named test |
|---|---|---|---|---|---|---|
| chat_header_left | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | `ChatView` chrome, first child of `chatColumnTopRef` | S | `chat:${chatId}:header-left` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_header_left` |
| chat_header_center | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | `ChatView` chrome, centered child of `chatColumnTopRef` | S | `chat:${chatId}:header-center` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_header_center` |
| chat_header_right | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | `ChatView` chrome, trailing child of `chatColumnTopRef` | S | `chat:${chatId}:header-right` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_header_right` |
| chat_top_dock | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | existing `chatTopDockRef` dock container | S | `chat:${chatId}:top-dock` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_top_dock` |
| chat_bottom_dock | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | chat column directly after `MessageList` | S | `chat:${chatId}:bottom-dock` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_bottom_dock` |
| chat_surface_side | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | sibling beside chat column before `PortraitPanel` | S | `chat:${chatId}:surface-side` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_surface_side` |
| chat_sidebar_left | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | left side surface in `ChatView` body | S | `chat:${chatId}:sidebar-left` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_sidebar_left` |
| chat_sidebar_right | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | right side surface in `ChatView` body | S | `chat:${chatId}:sidebar-right` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_sidebar_right` |
| chat_stream_before | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageList.tsx | first child inside `[data-chat-scroll=true]` | S | `chat:${chatId}:stream-before` | `DomDecoratorService`, then `MessageList` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_stream_before` |
| chat_stream_after | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageList.tsx | final child inside `[data-chat-scroll=true]` | S | `chat:${chatId}:stream-after` | `DomDecoratorService`, then `MessageList` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_stream_after` |
| chat_empty_state | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageList.tsx | empty-range branch before message rows | S | `chat:${chatId}:empty` | `DomDecoratorService`; empty branch unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_empty_state` |
| chat_composer_above | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx | first child of `InputArea` container | S | `chat:${chatId}:composer-above` | `DomDecoratorService`, then `InputArea` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_composer_above` |
| chat_composer_below | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx | final child of `InputArea` container | S | `chat:${chatId}:composer-below` | `DomDecoratorService`, then `InputArea` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_composer_below` |
| chat_input_tools_left | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx | input-bar tools row before native leading tools | S | `chat:${chatId}:input-tools-left` | `DomDecoratorService`, then `InputArea` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_input_tools_left` |
| chat_input_tools_right | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx | input-bar tools row after native trailing tools | S | `chat:${chatId}:input-tools-right` | `DomDecoratorService`, then `InputArea` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_input_tools_right` |
| chat_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx | action-bar container adjacent to native send controls | S | `chat:${chatId}:actions` | `DomDecoratorService`, then `InputArea` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_actions` |
| chat_toolbar | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx | toolbar row following the composer action bar | S | `chat:${chatId}:toolbar` | `DomDecoratorService`, then `InputArea` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts chat_toolbar` |
| message_header | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx and G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | first child of each message `.header` | R | `message:${message.id}:header` | `DomDecoratorService`; virtual row/message renderer unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_header by message id` |
| message_body_before | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx and G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | immediately before `MessageContent` | R | `message:${message.id}:body-before` | `DomDecoratorService`; virtual row/message renderer unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_body_before by message id` |
| message_body_after | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx and G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | immediately after `MessageContent` | R | `message:${message.id}:body-after` | `DomDecoratorService`; virtual row/message renderer unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_body_after by message id` |
| message_footer | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx and G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | final child of each message card | R | `message:${message.id}:footer` | `DomDecoratorService`; virtual row/message renderer unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_footer by message id` |
| message_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleActions.tsx | trailing child of `BubbleActions` pill | R | `message:${messageId}:actions` | `DomDecoratorService`, then `BubbleActions` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_actions by message id` |
| message_edit_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageEditArea.tsx | save/cancel action row after native buttons | R | `message:${messageId}:edit-actions` | `DomDecoratorService`, then `MessageEditArea` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_edit_actions by message id` |
| message_context_menu | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageList.tsx | keyed message-row context-menu portal owner | R | `message:${messageId}:context-menu` | `DomDecoratorService`; virtual `MessageList` row unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_context_menu by message id` |
| message_swipe_indicators | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx and G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | swipe indicator region beside native swipe controls | R | `message:${message.id}:swipe-indicators` | `DomDecoratorService`; virtual row/message renderer unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays message_swipe_indicators by message id` |
| landing_header | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/landing/LandingPage.tsx | `LandingPageHeader` header, before native actions | S | `landing:header` | `DomDecoratorService`, then `LandingPage` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts landing_header` |
| landing_hero | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/landing/LandingPage.tsx | first child of `LandingPageMain` | S | `landing:hero` | `DomDecoratorService`, then `LandingPage` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts landing_hero` |
| landing_characters | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/landing/LandingPage.tsx | existing `LandingPageCharacterPanel` region | S | `landing:characters` | `DomDecoratorService`, then `LandingPage` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts landing_characters` |
| landing_recent_chats | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/landing/LandingPage.tsx | `LandingPageChats` region | S | `landing:recent-chats` | `DomDecoratorService`, then `LandingPage` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts landing_recent_chats` |
| landing_footer | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/landing/LandingPage.tsx | final child of landing container | S | `landing:footer` | `DomDecoratorService`, then `LandingPage` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts landing_footer` |
| sidebar_top | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/ViewportDrawer.tsx | first child inside existing sidebar mount | S | `drawer:sidebar-top` | `DomDecoratorService`, then `ViewportDrawer` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts sidebar_top` |
| sidebar_bottom | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/ViewportDrawer.tsx | `sidebarBottom` before native bottom controls | S | `drawer:sidebar-bottom` | `DomDecoratorService`, then `ViewportDrawer` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts sidebar_bottom` |
| drawer_tab | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/ViewportDrawer.tsx | each mapped sidebar tab after native icon | R | `drawer-tab:${tab.id}` | `DomDecoratorService`; drawer-tab unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays drawer_tab by tab id` |
| drawer_header_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/ViewportDrawer.tsx | drawer header action cluster | S | `drawer:header-actions` | `DomDecoratorService`, then `ViewportDrawer` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts drawer_header_actions` |
| drawer_footer | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/ViewportDrawer.tsx | final drawer child below sidebar | S | `drawer:footer` | `DomDecoratorService`, then `ViewportDrawer` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts drawer_footer` |
| character_editor_tab | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/character-browser/CharacterEditorPage.tsx | editor tab strip after native tabs | S | `character-editor:${characterId}:tab` | `DomDecoratorService`; editor-page unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts character_editor_tab` |
| character_browser_card_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/character-browser/CharacterCard.tsx | card action cluster after native actions | R | `character-card:${character.id}:actions` | `DomDecoratorService`; card unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays character_browser_card_actions by character id` |
| preset_editor_tab | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/LoomBuilder.tsx | preset editor tab strip after native tabs | S | `loom:${presetId}:preset-tab` | `DomDecoratorService`, then `LoomBuilder` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts preset_editor_tab` |
| preset_editor_toolbar | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/LoomBuilder.tsx | preset-editor toolbar after native controls | S | `loom:${presetId}:preset-toolbar` | `DomDecoratorService`, then `LoomBuilder` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts preset_editor_toolbar` |
| persona_editor_tab | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/persona-browser/PersonaEditor.tsx | persona editor tab/action strip after native controls | S | `persona-editor:${personaId}:tab` | `DomDecoratorService`; persona-editor unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts persona_editor_tab` |
| world_book_entry_table | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/world-book-editor/EntryTable.tsx | table container before entry rows | S | `world-book:${bookId}:entry-table` | `DomDecoratorService`, then `EntryTable` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts world_book_entry_table` |
| world_book_entry_row | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/world-book-editor/EntryTable.tsx | each keyed entry row after row controls | R | `world-book-entry:${entry.id}:row` | `DomDecoratorService`; entry-row unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays world_book_entry_row by entry id` |
| world_book_entry_editor | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/shared/WorldBookEntryEditor.tsx | editor form before field body | S | `world-book-entry:${entryId}:editor` | `DomDecoratorService`; editor unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts world_book_entry_editor` |
| world_book_entry_toolbar | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/shared/WorldBookEntryEditor.tsx | entry editor toolbar after native actions | S | `world-book-entry:${entryId}:toolbar` | `DomDecoratorService`; editor unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts world_book_entry_toolbar` |
| lorebook_workspace | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/world-book-editor/LorebookEditorWorkspace.tsx | workspace root after header | S | `lorebook:${bookId}:workspace` | `DomDecoratorService`; workspace unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts lorebook_workspace` |
| lorebook_half_workspace | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx | half-workspace surface beside chat column | S | `chat:${chatId}:lorebook-half` | `DomDecoratorService`, then `ChatView` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts lorebook_half_workspace` |
| loom_builder_toolbar | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/LoomBuilder.tsx | main LoomBuilder toolbar after native controls | S | `loom:${presetId}:builder-toolbar` | `DomDecoratorService`, then `LoomBuilder` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts loom_builder_toolbar` |
| loom_builder_inspector | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/LoomBuilder.tsx | inspector pane before native inspector content | S | `loom:${presetId}:inspector` | `DomDecoratorService`, then `LoomBuilder` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts loom_builder_inspector` |
| regex_entry_row | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/RegexPanel.tsx | each keyed regex entry row after native controls | R | `regex-entry:${entry.id}:row` | `DomDecoratorService`; regex-row unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays regex_entry_row by entry id` |
| settings_tab | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/SettingsModal.tsx | sidebar dynamic-tab list after registered native tabs | R | `settings-tab:${tab.id}` | `DomDecoratorService`; settings-tab registry removal/modal unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays settings_tab by tab id` |
| settings_section | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/SettingsModal.tsx and G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/settings/ProductivitySettings.tsx | active settings content and each ProductivitySettings card boundary | R | `settings-section:${activeTabId}:${sectionId}` | `DomDecoratorService`; section/card unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays settings_section by tab and section id` |
| settings_card_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/settings/ProductivitySettings.tsx | `CardHeader` `cardHeaderAction` after native action | R | `settings-card:${cardId}:actions` | `DomDecoratorService`; settings-card unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx — `replays settings_card_actions by card id` |
| settings_extensions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/SettingsModal.tsx | active content extension-section container | S | `settings:extensions:${activeTabId}` | `DomDecoratorService`; modal/content unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts settings_extensions` |
| modal_header_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/shared/ModalShell.tsx | ModalShell header action cluster after native close control | S | `modal:${modalId}:header-actions` | `DomDecoratorService`, then `ModalShell` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts modal_header_actions` |
| modal_footer_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/shared/ModalShell.tsx | ModalShell footer action cluster after native actions | S | `modal:${modalId}:footer-actions` | `DomDecoratorService`, then `ModalShell` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts modal_footer_actions` |
| command_palette_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/CommandPalette.tsx | command-palette footer before shortcut hints | S | `command-palette:actions` | `DomDecoratorService`, then `CommandPalette` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts command_palette_actions` |
| manage_chats_actions | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/ManageChatsModal.tsx | header toolbar after native bulk/manage buttons | S | `manage-chats:${characterId}:actions` | `DomDecoratorService`, then `ManageChatsModal` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts manage_chats_actions` |
| prompt_variables_toolbar | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/shared/PromptVariablesModal.tsx | `PromptVariablesModal` header after title/subtitle | S | `prompt-variables:${presetId}:toolbar` | `DomDecoratorService`, then `PromptVariablesModal` unmount | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx — `mounts prompt_variables_toolbar` |

- [ ] Keep existing non-spec mount literals either as documented compatibility aliases or explicitly mark them as legacy; do not silently delete them.
- [ ] Replace the current singleton/exact-one-anchor assumption with catalog parity, cardinality, repeated-instance, virtualization replay, and unmount tests in `frontend/src/lib/spindle/mount-points.test.ts`, `frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx`, and `frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx`.
- [ ] Make the canonical-literal and physical-placement assertions independent and blocking: the catalog has exactly 58 canonical literals/58 literal records, while the placement manifest has exactly 64 physical JSX placements = 58 literal records + five second `MinimalMessageDefault` placements for `message_header`, `message_body_before`, `message_body_after`, `message_footer`, and `message_swipe_indicators` + one second `ProductivitySettings` placement for `settings_section`. A row that lists two files is one canonical literal record, not proof that either physical anchor is mounted.
- [ ] Preserve the 58 literal rows above unchanged. The following placement expansion disambiguates both sides of the six multi-file literal records (12 individually specified shared physical placements); together with the 52 single-file literal placements, it is the exhaustive 64-placement manifest. Every placement assertion records its exact file, JSX anchor region, cardinality and non-empty scope, and an individually named test assertion. The five shared-message literals require ten distinct physical-placement assertions: one against `BubbleMessageDefault.tsx` and one against `MinimalMessageDefault.tsx` for each literal; a single `mounts <literal>` assertion is insufficient.

| Literal / placement | Full absolute host path | Exact JSX insertion region / anchor | Card. / required stable scope | Exact test file and named assertion |
|---|---|---|---|---|
| message_header — `BubbleMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx | inside `<div className={styles.header}>`, immediately after native `<div className={styles.headerLeft}>` (avatar/name/`MetaPill`) | R; `message:${message.id}:bubble:header` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts BubbleMessageDefault physical placement for message_header` |
| message_header — `MinimalMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | inside `<div className={styles.header}>`, immediately after native `MetaPill` | R; `message:${message.id}:minimal:header` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts MinimalMessageDefault physical placement for message_header` |
| message_body_before — `BubbleMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx | inside the native `<div className={styles.content}>`, immediately before the `{isEditing ? ... : displayContent ? <MessageContent ...>}` branch | R; `message:${message.id}:bubble:body-before` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts BubbleMessageDefault physical placement for message_body_before` |
| message_body_before — `MinimalMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | immediately before the native `{isEditing ? ... : displayContent ? <MessageContent ...>}` branch | R; `message:${message.id}:minimal:body-before` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts MinimalMessageDefault physical placement for message_body_before` |
| message_body_after — `BubbleMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx | immediately after the native content `<div className={styles.content}>` and before the user-attachment conditional | R; `message:${message.id}:bubble:body-after` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts BubbleMessageDefault physical placement for message_body_after` |
| message_body_after — `MinimalMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | immediately after the native `{isEditing ? ... : displayContent ? <MessageContent ...>}` branch and before the user-attachment conditional | R; `message:${message.id}:minimal:body-after` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts MinimalMessageDefault physical placement for message_body_after` |
| message_footer — `BubbleMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx | inside `<div className={styles.bubble}>`, immediately after the `SwipeControls`/`GreetingNav` conditionals and before the sibling `BubbleActions` block | R; `message:${message.id}:bubble:footer` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts BubbleMessageDefault physical placement for message_footer` |
| message_footer — `MinimalMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | inside `<div className={styles.bubble}>`, immediately after the `SwipeControls`/`GreetingNav` conditionals and before the sibling `actionsWrap`/`MessageActions` block | R; `message:${message.id}:minimal:footer` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts MinimalMessageDefault physical placement for message_footer` |
| message_swipe_indicators — `BubbleMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessageDefault.tsx | inside `<div className={styles.bubble}>`, immediately after the native `SwipeControls` conditional and before the `GreetingNav` conditional | R; `message:${message.id}:bubble:swipe-indicators` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts BubbleMessageDefault physical placement for message_swipe_indicators` |
| message_swipe_indicators — `MinimalMessageDefault` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessageDefault.tsx | inside `<div className={styles.bubble}>`, immediately after the native `SwipeControls` conditional and before the `GreetingNav` conditional | R; `message:${message.id}:minimal:swipe-indicators` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts MinimalMessageDefault physical placement for message_swipe_indicators` |
| settings_section — `SettingsModal` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/SettingsModal.tsx | active `SettingsModal` settings-content boundary immediately after the selected tab heading and before native section content | R; `settings-section:${activeTabId}:modal` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts SettingsModal physical placement for settings_section` |
| settings_section — `ProductivitySettings` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/settings/ProductivitySettings.tsx | each keyed `ProductivitySettings` card boundary immediately after the card heading and before native card content | R; `settings-section:productivity:${cardId}` | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx — `asserts ProductivitySettings physical placement for settings_section` |

- [ ] In `dom-decorator-service.placements.test.tsx`, assert the 52 single-file literal placements plus these 12 shared placements total 64, not merely that 58 catalog rows exist; the manifest must name the ten shared-message anchors in `BubbleMessageDefault.tsx` and `MinimalMessageDefault.tsx` separately from the two `settings_section` anchors.

### Task 2.2: Build the centralized DomDecoratorService

- [ ] Add frontend/src/lib/spindle/dom-decorator-service.ts as a host-owned service over dom-helper.ts, dom-injection-registry.ts, extension-root-stamp.ts, and existing loader cleanup.
- [ ] Every React host anchor ref publishes an `AnchorRegistration` with the mount literal, a required non-empty stable scope, a unique live-anchor identity, owner, generation, DOM node, and optional instance key. `scope` is never optional for repeated, virtualized, or portal-backed mounts; singleton anchors still publish a deterministic host scope. Decorator options carry mount literal, render/update callback, priority, extension owner/generation, and optional instance key, but never infer scope from a document query.
- [ ] Define service ownership maps: `anchorsByLiveAnchorId`, `liveAnchorIdByNode` (WeakMap), `rootsByGenerationMountScope`, `registrationsByOwnerGeneration`, callback/disposer records by root, and legacy-observer entries by runtime. Root identity is `(owner,generation,mount,scope)`; the live-anchor identity distinguishes concurrent DOM nodes with the same mount/scope and prevents a second root for the same root identity/node pair.
- [ ] Require this registration order: ref attach allocates/reuses a live-anchor ID, validates the non-empty scope, updates the node map, records the registration, stamps/sanitizes the root, then queues the coordinated replay; an equivalent rerender updates the existing registration/callbacks without recreating a root. Ref detach/unregister removes callback and registration links first, disconnects/removes its observer entry, disposes injected nodes, clears root metadata, and then removes the root map/node-map records. Registration update and unregister are idempotent.
- [ ] Treat scope change and React node reuse as an atomic unregister-old/register-new transition: when one DOM node moves from scope A to B, remove scope-A root metadata, callbacks, observer entries, and injected nodes before accepting scope B; never mutate a root key in place. Apply the same sequence to virtualized recycling, portal removal/detachment, extension reload, and owner/generation unload. On generation unload, remove every matching root, callback, observer entry, injected node, and registration before the generation can register again; reload receives a fresh generation and cannot reuse stale callbacks or IDs.
- [ ] Use exactly one coordinated host-runtime replay/observer scheduler. Ref registration is the primary source of truth; MutationObserver observation is fallback-only for documented legacy non-React anchors, feeds that same scheduler, and must look up existing registration/root keys before creating anything. It cannot create duplicate roots. Preserve sanitizer checks, root stamping, callback ordering, idempotent disposer behavior, and loader/extension teardown integration.
- [ ] Sanitize HTML/SVG and reject forged extension-root metadata before insertion.
- [ ] Return an idempotent disposer and register it with extension generation teardown.
- [ ] Add `frontend/src/lib/spindle/dom-decorator-service.lifecycle.test.tsx` cases: `registers and unregisters a ref anchor with required scope`, `moves a reused React node from scope A to scope B without stale root`, `keeps repeated concurrent rows isolated by live-anchor identity`, `recycles virtualized anchors without stale callbacks`, `removes a detached portal anchor before replay`, `removes generation resources on unload and creates fresh resources on reload`, and `legacy observer fallback does not create duplicate root`. Add all physical placement assertions named in Task 2.1; no test may silently cover both BubbleMessageDefault/MinimalMessageDefault or SettingsModal/ProductivitySettings placements.

### Task 2.3: Separate Spindle overrides from persisted theme overrides

- [ ] Add an extension-owned override registry distinct from store.componentOverrides and theme-pack state.
- [ ] Spindle registrations are runtime-only, owner-and-generation-scoped entries in the new registry; they must never write, read back, merge with, or be persisted as `store.componentOverrides` or theme-pack overrides.

| Logical host / full absolute path | Safe public props (no private store or secrets) | Allowed slots / callbacks | Semantics | Priority and collision order | Native error fallback | Unload behavior | Exact behavioral test |
|---|---|---|---|---|---|---|---|
| BubbleMessage — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/BubbleMessage.tsx | frozen message view `{id,index,sendDate,role,isStreaming,isLastMessage}`, sanitized display content, swipe summary, attachment metadata; no private store or secrets | `Content`, `Reasoning`, `Attachments`, then `copy`, `edit`, `delete`, `fork`, `toggleHidden`, `toggleContextAnchor` | wrap default; replace only full visual shell | ascending priority, then registration sequence; same extension/id replaces its prior registration | per-override boundary renders `BubbleMessageDefault` | disposer removes generation entry and rerenders default | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `BubbleMessage wraps replaces falls back and unloads` |
| MinimalMessage — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MinimalMessage.tsx | same frozen message view and public metadata as BubbleMessage; no private store or secrets | `Content`, `Reasoning`, `Attachments`; same message actions | wrap default; replace full compact shell | ascending priority, then registration sequence | boundary renders `MinimalMessageDefault` | generation disposer restores default | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `MinimalMessage wraps replaces falls back and unloads` |
| MessageEditArea — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageEditArea.tsx | `{messageId, content, reasoning, canSave, isSaving}`; no private store or secrets | native editor slot; `setContent`, `setReasoning`, `save`, `cancel` | wrap editor controls; replace only with all callbacks retained | ascending priority, then registration sequence | boundary renders native `MessageEditArea` | generation disposer restores native editor and cancels no host state | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `MessageEditArea wraps replaces falls back and unloads` |
| InputArea — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx | `{chatId, draft, isGenerating, attachments}` public view; no private store, credentials, or secrets | composer/tools/actions slots; `setDraft`, `send`, `stop`, `openPromptVariables` | wrap default; replace composer only through supplied callbacks | ascending priority, then registration sequence | boundary renders native `InputArea` | disposer removes extension UI, preserves current native draft | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `InputArea wraps replaces falls back and unloads` |
| PortraitPanel — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/PortraitPanel.tsx | `{side,open,character:{id,name,avatarUrl},gallery}` public display data; no private store or secrets | portrait/gallery slots; `close`, `openImagePreview`, `selectAvatar` | wrap panel; replace frame only | ascending priority, then registration sequence | boundary renders native `PortraitPanel` | disposer returns visible panel to native rendering | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `PortraitPanel wraps replaces falls back and unloads` |
| QuickToolbar — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/quick-toolbar/QuickToolbar.tsx | `{actions:[id,label,icon,enabled], placement, density, docked}`; no private store or secrets | action rail/customizer slots; `invokeAction`, `openCustomizer` | wrap rail; replace only presentation while native action dispatch remains | ascending priority, then registration sequence | boundary renders native `QuickToolbar` | disposer restores action rail without changing persisted toolbar preferences | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `QuickToolbar wraps replaces falls back and unloads` |
| ConnectionsPicker — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/connections-picker/ConnectionsPicker.tsx | `{open,profiles:[id,name,provider,model],activeProfileId,models,layout}`; no private store, keys, tokens, or secrets | profile/model list slots; `selectProfile`, `selectModel`, `refresh`, `close` | wrap picker; replace presentation only | ascending priority, then registration sequence | boundary renders native `ConnectionsPicker` | disposer restores picker preserving selected profile/model | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `ConnectionsPicker wraps replaces falls back and unloads` |
| CharacterCard — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/character-browser/CharacterCard.tsx | `{character:{id,name,avatarUrl,description,tags}, selected}` public card data; no private store or secrets | card media/details/actions slots; `open`, `edit`, `toggleFavorite` | wrap card; replace visual card only | ascending priority, then registration sequence | boundary renders native `CharacterCard` | disposer returns each live card to native rendering | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `CharacterCard wraps replaces falls back and unloads` |
| LoomBuilder — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/panels/LoomBuilder.tsx | `{presetId,name,blocks,selectedBlockId,dirty}` public editor model; no private store or secrets | tabs, toolbar, inspector slots; `selectBlock`, `updateBlock`, `save` | wrap builder; replace only presentation with supplied commands | ascending priority, then registration sequence | boundary renders native `LoomBuilder` | disposer restores native builder without discarding edits | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `LoomBuilder wraps replaces falls back and unloads` |
| LandingPageShell (`LandingPage`) — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/landing/LandingPage.tsx | `{characters,recentChats,filters,loading}` public landing view; no private store or secrets | header, hero, characters, recent-chats, footer slots; `openCharacter`, `openChat`, `refresh` | wrap shell; replace presentation only | ascending priority, then registration sequence | boundary renders native `LandingPage` | disposer returns shell to native renderer | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `LandingPageShell wraps replaces falls back and unloads` |
| CommandPalette — G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/CommandPalette.tsx | `{open,query,commands:[id,label,group,shortcut]}` public command view; no private store or secrets | search/results/footer-action slots; `setQuery`, `execute`, `close` | wrap palette; replace presentation only | ascending priority, then registration sequence | boundary renders native `CommandPalette` | disposer restores native palette and removes extension commands | G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/component-override-registry.test.tsx — `CommandPalette wraps replaces falls back and unloads` |

- [ ] The override adapter wires only the 11 hosts listed in the table; a registration is rejected when it requests a non-public prop, an unavailable slot/callback, or a replace mode that omits the required supplied callbacks.
- [ ] Ensure overrides are removed on extension unload and never collide across extension generations.
- [ ] Add the named behavioral test in every table row for registration, wrap, replace, fallback/error, collision ordering, and unload.

### Task 2.4: Complete settings and message mount consumers

- [ ] Preserve the existing settings-tab bridge and SettingsModal dynamic sidebar behavior; add only missing SDK typing and mount anchors.
- [ ] Add settings_tab, settings_section, and settings_card_actions at stable component-owned anchors.
- [ ] Add message_actions and message_edit_actions without replacing native actions.
- [ ] Add registration and cleanup tests for settings tabs, sections, message actions, and hidden/disabled extensions.

### Task 2.5: Phase 2 security and performance gate

- [ ] Execute each Phase 2 matrix command exactly; the 58-canonical-literal assertion, the 64-physical-placement assertion, all named ref lifecycle cases, 11 override-host cases, decorator lifecycle cases, frontend compile, and checked build are separate blocking evidence. A row-count-only check or generic `mounts <literal>` case does not satisfy either placement or lifecycle evidence.
- [ ] Verify no extension-owned raw MutationObserver remains in lumiverse_suite.
- [ ] Verify DOMPurIFY coverage for every extension SVG/HTML sink, root ownership stamping, callback/observer/injected-node teardown for ref detach, scope change, portal detachment, generation unload, and reload, and no click-latency regression.
- [ ] Record the 58 canonical-literal count, 64 physical-placement count, root/observer/callback/injected-node zero counts after every teardown path, and bundle impact in the evidence ledger.

## Phase 3: Dynamic Provider and Driver Registries

### Task 3.1: Define canonical registry implementations and paths

- [ ] Embeddings: extend src/services/embeddings.service.ts while preserving known-provider behavior and preserving unknown-but-valid persisted IDs.
- [ ] TTS: extend src/tts/registry.ts and its service adapter; add owner-scoped register/unregister and change notifications.
- [ ] STT: extend src/services/stt-connections.service.ts and src/services/stt.service.ts; replace static provider lookup with the shared registry contract.
- [ ] Sidecar: extend src/services/memory-cortex/sidecar-adapter.ts, src/services/memory-cortex/index.ts, and src/services/memory-cortex/config.ts.
- [ ] Implement one authoritative provider registry in `G:/AI/All lumiverse repos/Lumiverse/src/spindle/manager.service.ts`, shared by backend services and frontend adapters. Its canonical public uniqueness key is exactly `{effectiveScope, installationId, kind, id}`, then `owner`, `generation`, and monotonic `recordRevision` are provenance/version fields. Same-owner same-generation identical registration is idempotent; the same key with a different owner is rejected; a newer activation generation atomically replaces only that installation/owner's stale record, while a stale generation or out-of-order revision is rejected. The record persists `{effectiveScope, installationId, kind, id, owner, generation, recordRevision, displayMetadata, capabilities, schemaVersion, descriptor, status}`. Derive scope and installation identity from the manager activation path around lines 1640–1652 and worker-host construction around lines 955–962; never accept worker-supplied user IDs, tenancy, installation identity, or ownership. Do not create separate embedding, TTS, STT, sidecar, backend, or frontend stores; adapters only project this registry.
- [ ] Treat provider descriptors, invocation requests, and invocation results as serializable data. Reject functions/callbacks, executable code, HTML, unknown fields, and arbitrary URLs at the registry boundary; callbacks remain inside the worker.
- [ ] Require descriptor validation for IDs, JSON shape, embedding dimensions, voice/model capabilities, endpoint `https:` scheme and configured URL/host allowlists, scope+installation/provider quotas, timeout limits, and duplicate ownership before a record enters the authoritative registry. Existing identifier-global grants are migrated by `102_spindle_provider_scope.sql` to scoped grants. The exact scope algorithm is `system` for an approved system worker, `operator:<approvedOperatorId>` for an approved operator worker, and `user:<authenticatedSubject>` for a user installation derived server-side; shared/operator workers cannot publish to user scopes. List/invoke receives an independently authenticated consumer user, resolves visibility from grants and scope, and never uses worker user IDs. Same `(kind,id)` is allowed only across different effective scopes or installations; the canonical key remains unique within its scope/installation.

### Task 3.2: Wire worker, backend, frontend, permissions, and lifecycle

- [ ] In `G:/AI/All lumiverse repos/lumiverse-spindle-types/src/permissions.ts`, declare exactly `providers.embedding.register`, `providers.tts.register`, `providers.stt.register`, and `providers.sidecar.register`; expose them through the SDK public permissions exports.
- [ ] Import the SDK-owned `WorkerToHostProviderMessage` and `HostToWorkerProviderMessage` in `G:/AI/All lumiverse repos/Lumiverse/src/spindle/worker-runtime.ts` and `src/spindle/worker-host.ts`; do not redeclare provider unions in either host file. `runtime-transport.ts` stays generic and accepts `unknown` for structural/size validation only. The direction is fixed: worker-to-host sends `provider_register`, `provider_unregister`, and `provider_result`; host-to-worker sends `provider_invoke`, `provider_abort`, and `provider_changed`.
- [ ] Wire the exact trust-boundary path and insertion points: in `worker-runtime.ts`, retain provider callbacks in a worker-only map adjacent to the existing handler maps before `const spindleApi` (line 1333), serialize only SDK DTO data through `post`, and handle host `provider_invoke`/`provider_abort`/`provider_changed` in `handleHostMessage` (line 4061); in `worker-host.ts`, derive effective scope and installation identity before `handleMessageInScope` from its existing scope construction around lines 955–962, validate unknown input before `handleMessageInScope` (line 1593), handle worker `provider_register`/`provider_unregister`/`provider_result` in the `switch` beginning at line 1601, and send host-originated SDK variants through `postToWorker` (line 1336). `manager.service.ts` derives the matching activation scope around lines 1640–1652 and owns registry mutation, owner+generation handles, grant/revocation, pending-call ownership, scoped list/invoke authorization, and registry emission.
- [ ] Require `manager.service.ts` and the worker-host validation gate to enforce the SDK DTO limits: 64 KiB descriptors, 256 KiB requests, 1 MiB results/errors, 1 MiB envelopes; reject malformed/non-JSON-safe values, functions, callbacks, code, HTML, prototype-polluting keys, unknown discriminators, duplicate scoped registry keys, stale owner/generation, and allowlist violations before dispatch. Registration and every invocation must verify the matching provider permission and the host-derived effective scope. Permission revocation, generation rollover, or extension unload immediately removes only matching `{effectiveScope, installationId, owner, generation}` records, aborts only their pending calls, suppresses their late results, and emits one scoped `provider_changed` only after the registry state is final.
- [ ] Implement the SDK correlation and lifecycle contract: require a non-empty correlation ID per invocation/result/abort; bind each pending call to `{effectiveScope, installationId, owner, generation, kind, id}` at host dispatch and start a 30,000 ms deadline. Timeout, caller cancellation, worker termination, extension unload, generation rollover, and permission revocation issue exactly one `provider_abort` to that owning worker, resolve only the authorized caller with the documented DTO error, mark the ID terminal, and suppress every subsequent `provider_result`. An abort or failed invocation must not affect another user, tenant, operator/shared installation, owner, generation, or provider record.
- [ ] Define the lifecycle algorithms precisely. `register` derives installation/scope/owner/generation, checks the scoped grant and descriptor, locks the canonical key, applies idempotent/replacement/conflict/revision rules, persists and commits before event fan-out. `list` independently authenticates the consumer, resolves scope visibility/grants, and returns only authorized live records with their revisions. `invoke` independently authenticates the consumer, resolves the live authorized record, creates the opaque pending-call binding before worker dispatch, and rejects stale/revoked records before any broker request. `revoke`/`disposeGeneration` atomically removes only matching records and pending calls, advances their removal revisions, commits, then emits resolved-recipient removals. `reconnect` authenticates the consumer, takes an authorized scoped snapshot plus revision watermark atomically, replaces the projection, and accepts only later revisions.
- [ ] Implement the two-stage host request broker for each of the six provider discriminators. Worker `prepare` may return only `BrokerRequest { correlationId, round, kind, id, method, url, headers, body, bodyEncoding: 'utf8' | 'base64', expectedResponseEncoding, timeoutMs, allowlistKey }`; the host validates method/URL/headers/body/size/allowlist, resolves the host-held secret, injects auth, performs the HTTP request, and binds it to an opaque pending-call token `{correlationId, round, effectiveScope, installationId, owner, generation}`. Worker `complete` receives only sanitized `BrokerResponse { correlationId, round, status, headers, body, bodyEncoding, contentType, ok }` and returns the final `provider_result`. Enforce cancellation/abort, one deadline, round matching, binary byte limits, header/body redaction, sanitized status/error mapping, and exactly-once completion; raw secrets, secret references, auth headers, and secret-bearing failures never reach worker, transport, event, projection, cache, or log. Add successful authenticated custom embedding/TTS/STT/sidecar tests plus timeout, abort, binary, redaction, and stale-round tests.
- [ ] Use host-mediated secret resolution only: `G:/AI/All lumiverse repos/Lumiverse/src/services/secrets.service.ts` resolves an opaque host-held secret immediately before the broker performs authenticated HTTP for `embeddings.service.ts`, `src/services/tts.service.ts`, `src/services/stt.service.ts`, or `src/services/memory-cortex/sidecar-adapter.ts`. The worker registration DTO, broker request/response, `provider_invoke` DTO, worker callback context, `provider_result`/error DTO, registry descriptor/projection, frontend store, cache key/fingerprint, WebSocket event, trace, and log contain no raw secret or secret reference. If resolution or access is denied, do not perform the request or post to the worker; return the documented redacted permission-denied/unavailable DTO and preserve isolation for other scopes.
- [ ] Define backend-to-frontend propagation through `G:/AI/All lumiverse repos/Lumiverse/src/ws/bus.ts`, not an unscoped broadcast: after each committed registry mutation, resolve visibility server-side for each recipient and emit one `provider_changed` envelope with explicit `recipientUserId` and `{ action: 'add' | 'remove' | 'change', effectiveScope, installationId, kind, id, owner, generation, recordRevision }`. Include `recordRevision` in every storage row, event, projection, removal result, and reconnect snapshot; reject an event or snapshot whose revision is not newer than the projected record, and reject stale generations. In `G:/AI/All lumiverse repos/Lumiverse/frontend/src/ws/useWebSocket.ts`, accept events only for the authenticated recipient and active authorized scope, apply add/remove/change to the scoped projection, and trigger embeddings, Voice Settings TTS/STT, and Memory Cortex sidecar consumers. On reconnect, call `ProviderManager.reconnect` for the authenticated user, replace the scoped projection atomically, then accept incremental events from the returned revision watermark; never accept a worker-supplied recipient or broadcast system event without resolved recipients.
- [ ] Add `src/spindle/provider-registry.test.ts`, `src/spindle/runtime-transport.provider-rpc.test.ts`, `src/spindle/worker-host.provider-rpc.test.ts`, and `src/spindle/provider-secret-resolution.test.ts` with named tests: `denies each provider registration permission`, `allows same kind and id across different effective users`, `rejects duplicate kind and id within one installation scope`, `does not trust worker supplied user or owner`, `isolates list and invoke across users and shared operator installations`, `enforces quotas per effective scope and installation`, `removes only matching scoped pending calls on revocation and unload`, `rejects stale owner generation handles`, `round trips each SDK provider discriminator without callbacks`, `rejects malformed and oversize provider envelopes`, `rejects stale generation and revoked permission before dispatch`, `aborts timed-out and cancelled invocations`, `suppresses late provider_result after abort`, `forwards one abort for timeout cancellation and unload`, `isolates a failed provider invocation`, `resolves embedding TTS STT and sidecar secret references only immediately before invocation`, `does not serialize secrets or secret references across provider transport results cache or logs`, and `returns redacted permission denied when host secret resolution is denied`. Add `G:/AI/All lumiverse repos/lumiverse-spindle-types/test/provider-runtime-union-parity.ts` — `keeps provider runtime discriminators directionally exhaustive` — to the SDK consumer fixture compiler.

### Task 3.3: Wire all provider consumers

- [ ] Connect backend embedding consumers in `src/routes/embeddings.routes.ts` and `src/routes/memory-cortex.routes.ts`, and frontend embedding/Cortex clients in `frontend/src/api/embeddings.ts` and `frontend/src/api/memory-cortex.ts`, to the active-scope projection of the authoritative registry. The projection key is `{effectiveScope, installationId, kind, id}` and carries `owner`, `generation`, and `recordRevision`; apply only monotonic revisions. Bind list/invoke to the separately authenticated consumer user after server-side visibility resolution. On `provider_changed`, retain a configured ID only while the matching owner+generation record is live; otherwise set the documented unavailable/fallback state without mutating the stored configured ID.
- [ ] Connect voice consumers through the verified paths `frontend/src/components/settings/VoiceSettings.tsx`, `frontend/src/api/tts.ts`, `frontend/src/api/tts-connections.ts`, `frontend/src/api/stt.ts`, and `frontend/src/api/stt-connections.ts`. `frontend/src/tts/registry.ts` and `frontend/src/api/voice.ts` do not exist and must not be introduced as parallel registries. Their scoped projection consumers must remove an unavailable TTS/STT option immediately on `remove`, replace metadata on `change`, append on `add`, and render unavailable/timeout fallback without closing or reloading Voice Settings.
- [ ] Make Memory Cortex render dynamic sidecar endpoints from the same scoped projection, preserve configured primary/fallback order, and remove a revoked/reloaded/unloaded sidecar from every selector while rendering the exact unavailable/timeout fallback rather than retaining a dead registration. On frontend reconnect, the embeddings API, voice APIs, and Memory Cortex API must wait for the scoped provider-registry resync before exposing refreshed option lists.
- [ ] Add `src/routes/embeddings.routes.provider-registry.test.ts`, `src/routes/memory-cortex.routes.provider-registry.test.ts`, `frontend/src/ws/useWebSocket.provider-registry.test.ts`, `frontend/src/components/settings/VoiceSettings.provider-registry.test.tsx`, `frontend/src/api/embeddings.provider-registry.test.ts`, `frontend/src/api/memory-cortex.provider-registry.test.ts`, and `frontend/src/api/voice-provider-registry.test.ts`. Name tests `register unregister and invoke embedding driver`, `register unregister and invoke TTS engine`, `register unregister and invoke STT engine`, `register unregister and invoke sidecar endpoint`, `emits scoped provider_changed add remove and change after registry commit`, `projects provider_changed only into the authorized frontend scope`, `suppresses stale generation provider_changed events`, `resyncs provider projection before applying reconnect events`, `removes embedding TTS STT and sidecar options after unload without page reload`, `renders unavailable and timeout fallback`, `denied registration is not visible to consumers`, and `provider failure is isolated`.
- [ ] Add cross-user and broker contract cases: `never accepts worker user IDs`, `shared operator worker publishes only to approved system/operator scope`, `user installation scope is derived server-side`, `binds authenticated consumer separately at list and invoke`, `same-owner same-generation registration is idempotent`, `same canonical key with different owner is rejected`, `newer generation atomically replaces only its own stale record`, `rejects out-of-order record revisions`, `reconnect snapshot carries revision watermark`, `successful authenticated custom embedding TTS STT and sidecar broker calls`, `worker never receives secret or secret reference`, `cancels opaque pending broker call`, `aborts stale round`, `redacts broker response and logs`, and `rejects binary/size/timeout violations`.
- [ ] Run `bun test src/spindle/provider-registry.test.ts src/spindle/runtime-transport.provider-rpc.test.ts src/spindle/worker-host.provider-rpc.test.ts src/routes/embeddings.routes.provider-registry.test.ts src/routes/memory-cortex.routes.provider-registry.test.ts`, then `bun test frontend/src/components/settings/VoiceSettings.provider-registry.test.tsx frontend/src/api/embeddings.provider-registry.test.ts frontend/src/api/memory-cortex.provider-registry.test.ts frontend/src/api/voice-provider-registry.test.ts`; record the exact exit codes and named-test results in the evidence ledger.

### Task 3.4: Make `lumiverse_suite` a public-SDK-only first-party consumer

- [ ] This is a blocking first-party-consumer requirement for invariants B and C as well as the 0.6.16 consumer requirement in A: after the packed `lumiverse-spindle-types@0.6.16` proof, every suite feature must use exported SDK types and public host surfaces only. The permitted contract is the public `SpindleFrontendContext`/module context plus its typed `ui`, `components`, `worldBooks`, `tokens`, settings, style, teardown, provider, and scoped-root handles; use public registrations and their returned disposers, `ctx.worldBooks.registerEntryDecorator`, and public `mountHostSurface`/host-surface handles. Do not augment SDK declarations, cast untyped host records to discover members, import private/internal host paths, inspect core selectors, acquire `document`/`window`/`globalThis.document`, or construct an extension-owned raw `MutationObserver`/`ResizeObserver`.
- [ ] The only DOM allowlist is rendering inside a root explicitly supplied by a typed public registration or `mountHostSurface` callback: a module may create, update, listen within, and clear only descendants of that root (or a public host-supplied portal/root handle), and must return the registration/host-surface disposer. It must not append to `document.body`/`head`/`documentElement`, attach document/window listeners, query outside that root, derive a root from parent/sibling/class/attribute selectors, use selector-based stale-node cleanup, or observe any document-wide/core node. Host-owned scoped-root observation, geometry, style injection, cleanup, and replay remain host responsibilities.
- [ ] Replace the following complete current-suite inventory. Each row is an independently blocking removal item; the stated replacement is the sole acceptable migration path and the named test must prove both the replacement and its removal condition.

| Inventory finding: file and symbol/line | Current behavior | Required public SDK API / host surface replacement | Removal condition | Exact test |
|---|---|---|---|---|
| `src/suite.ts` — `declare module 'lumiverse-spindle-types'` lines 33–38 and `removeOwnedModuleNodes` lines 172–195 (notably 177/191) | Augments `SpindleFrontendContext`, then scans document-wide extension roots/modules to remove stale nodes. | Export the required suite context members from SDK 0.6.16; use typed module registration/host-surface disposers and host teardown, with owner/generation cleanup performed by the host. | No `declare module 'lumiverse-spindle-types'`, `document.querySelector(All)`, or selector cleanup remains in `suite.ts`; unload invokes only typed disposers. | New `tests/suite-public-sdk-consumer.test.ts` — `uses only exported 0.6.16 SDK types` and `unload disposes every registration surface and scoped root`. |
| `src/modules/lorebook_workspace/content-resize.ts` — `installLorebookContentResizeTracking` lines 49–174 (constructors 53–58; document scan 126–131; mutation observer 146–163) | Obtains observers from `document.defaultView`, scans textarea selectors, and observes `document.documentElement` subtree/style mutations. | Public typed lorebook-workspace host surface supplies the scoped editor root and a host-managed resize/content-change subscription (and public geometry measurement if required). | No document/defaultView lookup, selector scan, `MutationObserver`, `ResizeObserver`, or `observe` call remains; host subscription disposer restores all tracked heights on unload. | Extend `tests/modules/lorebook-workspace-content-resize.test.ts` — `uses host scoped resize subscription and restores every textarea on dispose` and `host observer is disconnected before suite unload`. |
| `src/modules/lorebook_token_counts/index.ts` — `currentDocument`, `reconcile`, `activate`, `removeOwnedBadges` lines 132–135, 162–178, 274–293, 407–450 | Reads global document, scrapes lorebook row/cell selectors, mutates badges, and observes `doc.body`. | `ctx.worldBooks.registerEntryDecorator` plus typed entry payload/count APIs and its disposer; host owns row attachment, reconciliation, and observation. | No `Document`, global document, row/badge/native-cell selectors, `closest`, or `MutationObserver` remains; token cache/batch semantics survive registration and unload. | Extend `tests/modules/lorebook-token-counts-lifecycle.test.ts` — `registers entry decorator without row scraping`, `preserves cached batched counts`, and `unregisters decorator and leaves no callbacks on unload`. |
| `src/modules/portrait_dock/host-adapter.ts` — `sideLayoutElements` lines 580–612 and resize reclaim lines 920–942 | Walks parents and core chat selectors/classes, then installs a raw global `ResizeObserver` on host layout nodes. | Typed public portrait-dock layout/geometry host surface with scoped rect/resize subscription and disposer. | No parent/sibling traversal for host discovery, core selector/class query, `globalThis.ResizeObserver`, or raw observer construction remains. | Extend `tests/modules/portrait-dock-lifecycle.test.ts` — `uses portrait layout surface without core selectors` and `disposes host resize subscription on unload`. |
  | `src/modules/homepage_library/index.ts` — `mountPresentation` lines 51–83 | Uses structural casts, `ui.mount`, selector-based stale-root removal, guessed document/installation fields, and direct root append. | Typed public landing host-surface registration returning a scoped root and `SpindleHostSurfaceHandle`; use its update/destroy lifecycle. | No structural host casts, `querySelectorAll`, guessed `document`, or manual stale-root cleanup remains. | Extend `tests/modules/homepage-library-host-surface.test.ts` — `mounts only in supplied landing root and destroys host surface on unload`. |
  | `src/modules/character_display/settings-model.ts` — `CHARACTER_DISPLAY_DEFAULTS.visibleMetadata` line 55 | Coerces the frozen readonly `DEFAULT_VISIBLE_METADATA` array through `as unknown as CharacterDisplayMetadata[]` because the local settings model currently requires a mutable array, bypassing the static suite boundary rule. | Use the exported public SDK `CharacterDisplayMetadata` settings/defaults contract with a readonly-compatible typed settings value (or a typed host settings API that returns a fresh mutable copy), rather than casting the frozen shared default. | No `as unknown as` remains in `settings-model.ts`; the default metadata value is accepted through the declared public SDK/typed host settings API and callers cannot mutate the shared frozen default. | Extend `tests/modules/character-display-settings-model.test.ts` — `uses typed readonly metadata defaults without double cast`. |
  | `src/modules/lore_indicator/{mounts.ts,palette.ts,variants.ts}` — scoped cleanup, palette global listeners, `ownedBodyLayer` lines 76–82 and V2 body popover lines 134–170 | Discovers roots with selectors/`closest`, attaches document-level keyboard/pointer listeners, and appends body layers/popovers directly to `document.body`. | Public drawer/settings/overlay or portal host-surface registration supplies the scoped root, event/geometry/portal handle, and cleanup. | No root discovery selector, `document.body` append, document listener, or global document access remains; every overlay is owned by a returned public handle. | Extend `tests/modules/lore-indicator-teardown.test.ts` — `uses supplied overlay root and host event surface` and `unload destroys overlays listeners and handles`. |
| `src/shared/theme.ts` — `installThemeBridge` lines 29–56 | Creates a style tag and appends it to `document.head`. | Public scoped style registration/`ctx.styles` handle. | No `Document` parameter or `head` append remains; style lifetime is represented by a public disposer. | Extend `tests/shared/theme.test.ts` — `uses scoped style handle and removes it on final unload`. |
| `src/modules/{connections_picker/runtime.ts,quick_toolbar/controller.ts,quick_toolbar/toolbar.ts}` — active-element/global document path at `runtime.ts:245`, toolbar/controller selector paths around `controller.ts:246` and `toolbar.ts:170` | Uses document state or selector lookup to control host-mounted toolbar/picker descendants. | Typed event/focus/action surface and the explicitly supplied scoped renderer root; retain only root-local refs created by the renderer, not selector lookup. | No document/global focus access or `querySelector(All)`/`closest` appears in these modules. | Extend `tests/modules/connections-picker-runtime.test.ts` — `uses host focus surface without document access`; extend `tests/modules/quick-toolbar-dom.test.ts` — `uses renderer refs without selector lookup`. |
| `src/modules/{character_display/host-adapter.ts,connections_picker/host-adapter.ts,lore_indicator/host-adapter.ts,lorebook_workspace/index.ts,quick_toolbar/host-adapter.ts,shared/productivity-host-surface.ts}` — fallback interfaces, `UnknownRecord`/structural member probing and optional private-like host APIs | Re-declares or discovers host capabilities with casts and fallback interfaces rather than compiling against SDK declarations. | Import the corresponding exported 0.6.16 SDK types and call only their declared members/returned handles. | Remove fallback interfaces, `UnknownRecord`/`as unknown as` host capability probes, optional ad-hoc host member definitions, and private settings reads/watches; no private/internal imports. | New `tests/suite-public-sdk-boundary.static.test.ts` — `rejects fallback host contracts private imports and declaration augmentation`; new `tests/suite-public-sdk-consumer.test.ts` — `provider registration and unload use public typed handles`. |
| `src/modules/{character_display/{index.ts,runtime.ts},character_library_scope/index.ts,connections_picker/{host-adapter.ts,runtime.ts},lore_indicator/{panel.ts,popover.ts,settings-view.ts},lorebook_workspace/index.ts,portrait_dock/runtime.ts,quick_toolbar/{context-cards.ts,customize.ts,toolbar.ts}}` — direct element construction/attachment | Builds extension UI through raw DOM APIs; some paths are currently fed an unscoped/guessed document. | Keep only rendering through a typed host-provided scoped root (or move renderer implementation behind the public `mountHostSurface` callback); all external attachment is host owned. | Every remaining DOM operation is mechanically attributable to a typed supplied root; no module has a global `document`/`window`, body/head/documentElement attachment, or core selector. | New `tests/suite-public-sdk-boundary.static.test.ts` — `permits only host-provided scoped-root rendering`; new `tests/suite-public-sdk-consumer.test.ts` — `all modules receive and release only scoped host roots`. |

- [ ] Add `tests/suite-public-sdk-boundary.static.test.ts`, named exactly `scans the entire suite source for forbidden host-DOM and SDK-boundary patterns`, as a deterministic recursive scan of `src/**/*.{ts,tsx}` (including every file in the inventory). Run it with `bun test tests/suite-public-sdk-boundary.static.test.ts`. It must fail on: `declare module 'lumiverse-spindle-types'`; imports containing `/internal` or `/private`, then `MutationObserver`, `ResizeObserver`, `.observe(`, `document.`/`window.`/`globalThis.document`, then `querySelector`, `querySelectorAll`, `getElementById`, `getElementsBy`, `.closest(`, then `document.body|head|documentElement`, then `UnknownRecord`; host capability `as unknown as` casts; and ad-hoc `host`/`ui`/`components` fallback interfaces. The test may allow DOM construction/listener operations only when the module receives a typed public `ScopedHostRoot`/host-surface root parameter and the operation targets that parameter or its descendant; it must reject all other DOM roots and every core selector. Keep the allowlist as explicit file/symbol/line assertions in that test so a new exception requires a reviewable test edit, never a wildcard.
- [ ] Add `tests/suite-public-sdk-consumer.test.ts` with exact cases `uses only exported 0.6.16 SDK types`, `registers and unloads provider through public handles`, `mounts only in a host-provided scoped root`, and `unload disposes every registration surface and scoped root`. The provider case must prove no registration/callback survives unload; the root case must prove no document/root discovery was required. Extend the resize, token-count, portrait, homepage, lore-indicator, picker, toolbar, and theme tests named in the inventory; each must assert zero extension-owned observers/listeners/roots after its disposer and suite unload.
- [ ] Compile the actual suite against the packed SDK, not a workspace/source resolution. After the SDK Phase 0 tarball is produced, install `lumiverse-spindle-types-0.6.16.tgz` into `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite/.tmp-packed-sdk-consumer` with a fixture `package.json`/`tsconfig.json` that imports the built suite public entry and `lumiverse-spindle-types` package root; run `npx --prefix .tmp-packed-sdk-consumer tsc -p tsconfig.json --noEmit`, assert `.tmp-packed-sdk-consumer/node_modules/lumiverse-spindle-types/dist/index.d.ts` exists, then remove only that temporary fixture. The consumer fixture must compile with no declaration merging, private path, or source-directory resolution. Do not remove any suite fallback until this command and the SDK packed fixture pass.
- [ ] Phase 3.4 passes only when the entire inventory is removed or constrained to the explicit scoped-root allowlist, the static boundary test and all named runtime lifecycle tests pass, the suite resolves registry `lumiverse-spindle-types@0.6.16`, and the packed consumer compile proves the emitted public declarations. Generic “refactor suite” evidence does not satisfy this gate.

### Phase 3 gate

- [ ] C1 through C5 are green in the evidence ledger.
- [ ] A provider registered by an extension is absent immediately after that extension unloads.

## Phase 4: Toolbar, Docker Actions, and Connection Picker

### Task 4.1: Complete persisted state and defaults

- [ ] Update frontend/src/types/store.ts, frontend/src/store/slices/settings.ts, frontend/src/lib/uiProductivityDefaults.ts, and ProductivitySettingsModel.ts with typed fields for:
  - auto-fit toolbar bounds
  - gap and padding
  - top docking and snap mode
  - V2 icon-only mode
  - stable toolbar order and visibility
  - chat action-bar order, visibility, placement, and missing-action handling
  - ConnectionsPicker modelLayout
- [ ] Add `activeProfileId` and `connectionsPickerSettings.modelLayout` to `DATA_KEYS`, defaults, normalization, migration, and the typed `StartupSettings` contract in both frontend/src/types/store.ts and src/services/bootstrap.service.ts. Persist only semantic IDs and the `grid`/`list` enum; never persist a DOM selector, generated class name, or transient request state.
- [ ] Keep `selectedProfileId` picker-local and derive its initial/reopen value from hydrated `activeProfileId`; the picker must never be the second source of truth for the active profile.
- [ ] Use stable semantic mount IDs and action IDs; do not persist generated CSS class names.

### Task 4.2: Fix geometry and zero dead space

- [ ] Define one persisted canonical `quickToolbarPlacement: 'floating' | 'chat_top_dock'` enum in the existing productivity settings/defaults/normalization/migration path. The suite chooses only this target state; `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite/src/modules/quick_toolbar/index.ts` (around line 12) must stop deriving a mount target from the V1/V2 visual variant. `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/quick-toolbar/QuickToolbar.tsx` (around lines 176-177) consumes the enum as the sole `freePosition`/anchored decision, and `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx` (around lines 1095-1104) remains the one singleton top dock that also contains native Select Messages content. V1/V2 changes presentation only: neither chooses a different dock target nor creates a second mount.
- [ ] Update frontend/src/lib/quickToolbarGeometry.ts and frontend/src/components/quick-toolbar/QuickToolbar.tsx to distinguish auto dimensions from user-pinned dimensions and use one toolbar-root `ResizeObserver`, not one observer per action. The single dock owner measures and publishes the in-flow dock contribution; QuickToolbar must consume that ownership rather than installing a competing dock observer.
- [ ] Implement this measurement/update algorithm: observe the rendered action rail; on child mount/unmount, hidden-state change, async icon/label completion, orientation change, V1/V2 switch, gap/padding change, or root resize, enqueue one `requestAnimationFrame` batch. In that batch read the rail's `scrollWidth`/`scrollHeight` and computed leading/trailing padding and gap, calculate the natural main-axis extent from visible children only, clamp it to the configured minimum accessibility size and viewport-safe maximum, and write the auto main-axis size plus the cross-axis content-fit size. Vertical uses height as main axis and horizontal uses width; a user-pinned axis is never overwritten. Empty or all-hidden children resolve to the clamped minimum rather than retaining the last non-empty measurement.
- [ ] Cancel a queued animation frame before scheduling a newer one; temporarily disable/cancel the size transition before the synchronous geometry write, force the new value, and then restore the transition on the next frame so interrupted CSS transitions cannot leave stale dead space. On placement, variant, reload, viewport, unmount, or extension-unload change, disconnect obsolete observers, cancel pending animation frames, clear stale `--lcs-top-dock-height`, and clear transient inline size/transition state. No stale dock height, duplicate geometry observer, or residual in-flow reservation may survive.
- [ ] Use content-fit flex sizing and configurable gap/padding; preserve the configured min/max and viewport constraints. In `chat_top_dock` mode, order the toolbar and native Select Messages content explicitly and give each child its measured available width; in floating mode, remove the dock contribution and leave zero dead space. The invariant is one top-dock anchor, zero unused main-axis space after every settled update (within one device pixel), and at most one measure-and-write geometry batch per animation frame.
- [ ] Add the persisted-enum migration/default: absent or legacy placement defaults deterministically to the existing floating behavior; invalid values normalize to `floating`; reload preserves a valid placement. Keyboard focus, tab order, and accessible names must remain correct when entering/leaving the in-flow dock, and docked controls must not introduce a keyboard trap.
- [ ] Extend `frontend/src/components/chat/chatDockContract.test.ts` with `reuses the singleton chat_top_dock anchor`, `maintains one dock observer across child changes`, `keeps Select Messages and docked toolbar order and width in flow`, and `clears stale --lcs-top-dock-height after undock`. Extend `frontend/src/lib/spindle/geometry-api.contract.test.ts` with `fits horizontal and vertical visible children without dead space`, `clamps empty hidden and async children without retaining stale size`, and `normalizes quickToolbarPlacement migration and default`. Add `frontend/src/components/quick-toolbar/QuickToolbar.geometry.test.tsx` cases `coalesces resize updates to one layout batch per frame`, `does not overwrite a pinned axis`, `renders exactly one top dock for V1 and V2 across floating and chat_top_dock placement`, `preserves dock placement through reload and viewport change`, `gives dock children correct order width and zero dead space`, `keeps docked keyboard navigation and accessible names`, and `cancels observer and animation frame work on unload`; assert zero duplicate anchors/observers and no more than one layout update per frame. Execute the full V1×V2 × floating/docked × reload/viewport-change matrix in those named cases.

### Task 4.3: Implement stable top docking and magnetic snapping

- [ ] Reuse and standardize the existing singleton `<div ref={chatTopDockRef} ... data-spindle-mount="chat_top_dock">` in `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/ChatView.tsx` (around lines 1095-1104); do not insert a second `chat_top_dock` anchor. `chatTopDockRef` is the only dock-target measurement, geometry-observer, CSS-variable, ordering, width-allocation, and lifecycle owner for both variants and native Select Messages content.
- [ ] Interpret only canonical `quickToolbarPlacement`: `chat_top_dock` renders the active V1 or V2 presentation as an in-flow child of that singleton anchor, while `floating` is detached from in-flow sizing. In the same animation-frame batch, a change removes the previous mode's contribution before publishing the next one. Clear `--lcs-top-dock-height` whenever the dock has no measured child. A visual V1/V2 change must retain the placement and must never remount into a variant-specific target.
- [ ] Update the suite target selection in `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite/src/modules/quick_toolbar/index.ts` (around line 12), the host `freePosition`/anchored predicate in `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/quick-toolbar/QuickToolbar.tsx` (around lines 176-177), and the productivity-host-surface consumer so all use the same persisted enum and the existing ChatView singleton dock.
- [ ] Extend the geometry hook with dock target rect and snap threshold; preserve existing edge snapping.
- [ ] Implement reordering only after a continuous 2,000 ms long-press. Before that threshold, pointer/touch activation remains a normal click; cancel the pending hold on `pointerup`, `pointercancel`, `touchend`, `touchcancel`, leaving the toolbar, Escape, blur, scroll, or an incompatible second pointer. While dragging, prevent the post-drag click, capture/release the active pointer, restore focus to the moved action, and persist order only after a successful drop. Keyboard reordering must use an explicit grab command (Space/Enter), Arrow keys to move, Escape to cancel, and an aria-live position announcement; it must never require a long press.
- [ ] Persist the ordered stable IDs through the existing settings path, normalize duplicates/missing/unregistered IDs on startup and extension reload, append newly registered actions by catalog order, and unregister action listeners, timers, pointer capture, document listeners, and drag state on unload.
- [ ] Extend `frontend/src/components/chat/chatDockContract.test.ts` with `docks V1 and V2 into the existing singleton anchor without reserved floating space`, `removes dock contribution on undock reload and viewport change`, and `keeps exactly one top-dock anchor across the V1 V2 placement matrix`. Add `frontend/src/components/quick-toolbar/QuickToolbar.reorder.test.tsx` with `starts pointer drag only after 2000ms`, `cancels pointer touch and keyboard pending drag paths`, `persists successful reorder and restores focus`, and `unload clears timers listeners and pointer capture`. The dock matrix must prove V1×V2 × floating/docked × reload/viewport-change has exactly one anchor, no stale `--lcs-top-dock-height`, no duplicate observers, correct Select Messages/toolbar order and child widths, and zero dead space.

### Task 4.4: Real icons and accessible icon-only mode

- [ ] Add DynamicExtensionIcon using existing DOMPurify policy for SVG and safe image URL handling.
- [ ] Preserve icon data through spindle-placement.ts and drawer-tab-registry.tsx into useQuickToolbarActions.ts.
- [ ] Update QuickToolbar, QuickToolbarCustomizeModal, overflow, and inline consumers. In V2 icon-only collapse, retain each action's visible tooltip trigger, `aria-label`, keyboard focusability, and focus ring; labels may be visually hidden but must remain available to accessible-name computation. Keep square hit targets at or above the accessibility minimum and re-run the Task 4.2 geometry batch when collapse/expand changes the visible content, so icon-only mode creates no dead-space regression.
- [ ] Extend frontend/src/components/quick-toolbar/QuickToolbar.geometry.test.tsx with `V2 icon-only collapse retains accessible names focus and zero dead space`; add icon tests named `sanitizes valid malformed and unsafe extension icons` and `recalculates overflow after icon-only collapse`.

### Task 4.5: Shared eight-action catalog and reorder behavior

- [ ] Create one stable chat docker action catalog and invocation bridge consumed by frontend/src/lib/commands.ts, frontend/src/components/chat/InputArea.tsx, QuickToolbar, and the action-bar customizer. Do not copy callbacks into the catalog: entries invoke the existing owners through a current InputArea command bridge or the existing global command implementation.

| Action ID | Label / icon | Existing owner, file, and symbol | Capability / permission | Availability and fallback | Default order | Named test |
|---|---|---|---|---|---:|---|
| `chat.new` | New Chat / `Plus` | frontend/src/lib/commands.ts `action-new-chat` (lines ~102-110); active-chat invocation delegates to frontend/src/components/chat/InputArea.tsx `createNewChat` (line ~2335) | global navigation; active-chat write | global command is available always; active-chat variant requires a live chat and otherwise uses the global command | 1 | `catalog exposes chat.new once and invokes the existing new-chat owner` |
| `chat.manage` | Manage Chats / `FolderOpen` | frontend/src/lib/commands.ts `action-manage-chats` (lines ~196-207) | active character/chat context | disabled without `activeCharacterId`; do not synthesize a replacement modal | 2 | `catalog invokes existing manage-chats command only when its scope is available` |
| `chat.prompt-variables` | Prompt Variables / existing InputArea prompt-variables icon | InputArea.tsx `openPromptVariablesModal` (line ~1071) | active Loom preset; no extension permission beyond the first-party command bridge | disabled while loading or without `activeLoomPresetId`, with the existing informational fallback | 3 | `catalog invokes openPromptVariablesModal and preserves preset availability` |
| `chat.settings` | Chat Settings / `Settings2` | InputArea.tsx `openModal('chatSettings', ...)` (line ~3379) | active chat and modal service | disabled without a live chat; preserve fetch/error handling before opening the modal | 4 | `catalog opens existing chatSettings modal` |
| `chat.convert-to-group` | Convert to Group Chat / existing group icon | InputArea.tsx `handleConvertToGroup` (line ~3428) | non-group active chat; chat mutation | hidden/disabled for group chats or unavailable chat; no duplicate conversion path | 5 | `catalog invokes handleConvertToGroup only for eligible chats` |
| `chat.new-group` | New Group Chat / existing group-plus icon | InputArea.tsx `openModal('groupChatCreator')` (line ~3442) | modal service | available when the group-creator modal is registered; otherwise disabled | 6 | `catalog opens existing groupChatCreator modal` |
| `chat.authors-note` | Author's Note / existing note icon | InputArea.tsx `setAuthorsNoteOpen(true)` (line ~3455) | active chat composer state | disabled without an active chat; do not create a parallel note state | 7 | `catalog opens existing authors-note state` |
| `chat.recompile-memories` | Recompile Memories / existing memory icon | InputArea.tsx `memoryCortexApi.warm(chatId, { force: true })` (line ~3470) | active chat plus Memory Cortex availability | disabled when Cortex is unavailable/in flight; preserve existing toast and result/error behavior | 8 | `catalog invokes existing forced Memory Cortex warm path` |

- [ ] The table is exactly the eight default IDs. `chat.new` is the sole collision bridge for both `action-new-chat` and contextual `createNewChat`; no additional `new` action may register. `chat.manage` maps only to `action-manage-chats`. Resolve duplicate IDs by retaining the built-in catalog entry, then order visible actions by persisted IDs, followed by catalog default order, then extension registration sequence; reject a second registration of any built-in ID.
- [ ] Register translations for all eight IDs across supported command locales and preserve each owner's disabled, loading, and error states. Both V1 and V2 consume the same filtered catalog and sortable wrapper; extension actions may append only after the eight defaults and must unregister without leaving an order hole.
- [ ] Add frontend/src/components/chat/chatDockerActionCatalog.test.ts with the eight named table tests plus `every default action appears exactly once in V1 V2 and the customizer`, `catalog actions respect capabilities and owner availability`, `collision and persisted-order rules are deterministic`, and `unregister removes action invocation and persisted placeholder cleanly`.

### Task 4.6: Connection Picker persistence and model UX

- [ ] Keep the picker owner/render path in frontend/src/lib/spindle/productivity-host-contracts.tsx: `connections_picker.launcher` creates the launcher (lines ~460-472) and `connections_picker.panel` renders `<ConnectionsPicker ...>` (lines ~474-475). Do not create a page-level picker owner or a second launch state.
- [ ] Keep the public bridge additive and source-compatible. In `G:/AI/All lumiverse repos/Lumiverse/frontend/src/lib/spindle/frontend-domain-api.ts`, retain the existing `connections.setActive(id: string | null): void` signature and synchronous fire-and-forget behavior for legacy extensions; it delegates safely to the coordinator without exposing a rejected promise. Add `connections.setActiveAcknowledged(id: string | null): Promise<void>` as the V2/acknowledged capability-gated method, with the same generation permission check, and make it await the coordinator. Do not change, replace, or widen the legacy `setActive` signature; this is an additive member under Task 0.2's SDK signature policy.
- [ ] Make `G:/AI/All lumiverse repos/Lumiverse/frontend/src/store/slices/connections.ts` (around lines 17-28) `ConnectionsSlice.setActiveProfile` the sole global acknowledged active-profile mutation coordinator. It accepts an explicit reason and returns the persistence acknowledgement promise. It owns one serialized request queue plus a monotonically increasing request sequence/generation: validate the candidate against the current stable backend list; capture the prior acknowledged profile/reasoning snapshot; stage and persist the candidate profile and its reasoning settings as one acknowledgement unit; commit only the latest still-valid generation after its acknowledgement; and suppress stale A→B responses. On rejection, restore/retain the prior acknowledged snapshot atomically and expose an actionable error; no caller may optimistically commit global `activeProfileId` or reasoning state. Do not let a component, bootstrap path, deletion handler, settings form, or public bridge write `activeProfileId` directly.
- [ ] Migrate and explicitly enumerate every caller to await `ConnectionsSlice.setActiveProfile`: the new `connections.setActiveAcknowledged` bridge method; legacy bridge `connections.setActive` as a safe non-awaiting compatibility delegate; `frontend/src/App.tsx` pending-connections deep link (around line 235); `frontend/src/components/chat/InputArea.tsx` connection-popover selection (around line 3319); `frontend/src/components/panels/ConnectionManager.tsx` active-toggle selection (around line 235); and `frontend/src/components/connections-picker/ConnectionsPicker.tsx` `selectProfile` and post-model-update selection (around lines 294 and 302). Also migrate `frontend/src/hooks/useAppInit.ts` bootstrap reconciliation, connection-profile create/edit/delete handlers, every profile/settings selector action, and provider-registry/reconnect reconciliation that can invalidate the active profile. Each acknowledged call passes `user_selection`, `bootstrap_reconcile`, `profile_deleted`, `profile_invalidated`, or `settings_reconcile`; the legacy bridge preserves its synchronous API while safely delegating. Before migration, record the repo-wide inventory of direct `activeProfileId` writes and `setActiveProfile` calls; after migration, the coordinator and its controlled hydration path are the only permitted writers and no bypass remains.
- [ ] Correct the picker race in `frontend/src/components/connections-picker/ConnectionsPicker.tsx`: local `selectedProfileId` initializes/reconciles from the coordinator's acknowledged `activeProfileId`, including open/reopen, rather than independently resetting at lines ~105-117 and ~169-173. On cold start/open/reopen, retain the hydrated acknowledged value when present; only the coordinator may select and acknowledge the stable-first profile fallback, and only `null` when no profiles exist.
- [ ] Add `activeProfileId` to backend `src/services/bootstrap.service.ts` `StartupSettings` and `STARTUP_SETTINGS_KEYS`, serialize it through `src/routes/bootstrap.routes.ts`, type it in `frontend/src/api/bootstrap.ts` and `frontend/src/types/store.ts`, hydrate it through `frontend/src/store/slices/settings.ts`, and reconcile it in `frontend/src/hooks/useAppInit.ts` by awaiting the coordinator. Bootstrap/startup-settings failure preserves the last acknowledged profile/reasoning snapshot and must not replace it with an empty/default picker value.
- [ ] On deletion during an in-flight switch, invalidate and remove the affected queued/current generation before any later acknowledgement can commit it; if the deleted profile was pending or acknowledged, enqueue the stable first remaining profile (or `null` only when empty) as the deletion fallback, atomically bind its reasoning settings, await its acknowledgement, clear the deleted profile's model cache, and prevent the late switch response from restoring the deleted selection. Reopen/reload reads only the acknowledged backend value after a successful coordinator commit.
- [ ] Keep `selectedProfileId` a primitive; fetch models only from `(selectedProfileId, refreshGeneration)`, never a mutable profile object. Use a per-profile cache with invalidation on successful dedicated refresh, credential/profile update, registry change, and profile deletion. Abort a prior `AbortController` whenever profile, refresh generation, or picker lifecycle changes; apply a response only if its request token and selected profile still match, so stale refresh responses cannot overwrite a newer profile.
- [ ] Persist `modelLayout: 'grid' | 'list'` through store defaults/migration, Productivity Settings, and ConnectionsPicker rendering. Add real-time provider-scoped model filtering with a 150 ms debounce, case-insensitive matching on model ID and label, result count, empty state, and keyboard-accessible input/list navigation. Add a dedicated refresh button with disabled/loading state, error message, and last-successful-refresh timestamp; it is separate from changing profile or reopening the picker.
- [ ] Extend `frontend/src/lib/spindle/productivity-host-contracts.test.tsx` (create it if no contract test exists) with `renders ConnectionsPicker only through connections_picker.panel and launcher contract`. Add `frontend/src/lib/spindle/ctx-domain-api.test.ts` case `keeps legacy connections.setActive synchronous while setActiveAcknowledged awaits coordinator acknowledgement`, proving the legacy signature remains `void`, permission denial is preserved, the additive method resolves only after the coordinator acknowledgement, and a legacy persistence rejection is contained. Add `frontend/src/store/slices/connections.active-profile.test.ts` cases `awaits backend acknowledgement before committing active profile and reasoning settings`, `rolls back active profile and reasoning settings when persistence fails`, `serializes overlapping A to B switches and suppresses stale acknowledgement`, and `deletion during an in-flight switch cannot restore the deleted profile`. Add caller-migration tests: `frontend/src/App.active-profile.test.tsx` — `awaits acknowledgement for a pending connections deep link`, then `frontend/src/components/chat/InputArea.active-profile.test.tsx` — `awaits profile acknowledgement before closing the connection popover`, then `frontend/src/components/panels/ConnectionManager.active-profile.test.tsx` — `awaits coordinator acknowledgement for active toggle selection`; and `frontend/src/components/connections-picker/ConnectionsPicker.persistence.test.tsx` — `picker awaits coordinator acknowledgement before reflecting selection`, `post-model-update selection awaits coordinator acknowledgement`, `persists an acknowledged profile switch across close and reopen`, `falls back through the coordinator after selected profile deletion`, `uses grid and list layout persistently`, `filters generated and registered provider models case insensitively with keyboard access`, `shows the no-provider state`, and `aborts stale refresh responses while preserving loading error and last-refresh state`. Add `frontend/src/hooks/useAppInit.bootstrap-active-profile.test.ts` cases `hydrates activeProfileId from bootstrap and preserves it across reopen` and `preserves last acknowledged profile and reasoning settings when cold-start bootstrap fails`; add `src/services/bootstrap.service.active-profile.test.ts` with `includes activeProfileId in StartupSettings and STARTUP_SETTINGS_KEYS`. These cases are required evidence for the additive extension bridge, every enumerated picker/non-picker caller migration, successful acknowledgement, failed-persistence rollback, serialized overlap, deletion fallback, bootstrap failure, reopen/reload, and stale-response suppression.

### Phase 4 gate

- [ ] D1 through D8 and E1 through E5 are green.
- [ ] Run `bun test frontend/src/components/chat/chatDockContract.test.ts frontend/src/lib/spindle/geometry-api.contract.test.ts frontend/src/components/quick-toolbar/QuickToolbar.geometry.test.tsx frontend/src/components/quick-toolbar/QuickToolbar.reorder.test.tsx frontend/src/components/chat/chatDockerActionCatalog.test.ts frontend/src/lib/spindle/productivity-host-contracts.test.tsx frontend/src/components/connections-picker/ConnectionsPicker.persistence.test.tsx frontend/src/hooks/useAppInit.bootstrap-active-profile.test.ts src/services/bootstrap.service.active-profile.test.ts`, then `npm --prefix frontend run typecheck` and `npm --prefix frontend run build:checked`; record named-test results, the one-layout-update-per-frame threshold, and duplicate-anchor/observer counts in the evidence ledger.

## Phase 5: Embeddings, Memory Cortex, Settings, and Edit and Send

### Task 5.1: Embedding profiles and fallback

- [ ] Add an additive `connectionProfiles`/`primaryProfileId`/`fallbackProfileIds` shape to the persisted `embeddingConfig` owned by `G:/AI/All lumiverse repos/Lumiverse/src/services/embeddings.service.ts` (`EMBEDDING_SETTINGS_KEY = "embeddingConfig"`), its `EmbeddingConfig` API type, and the request schema in `G:/AI/All lumiverse repos/Lumiverse/src/routes/embeddings.routes.ts`. A profile contains a stable generated UUID, provider ID, model, endpoint/Vertex project and region options, requested dimensions, enabled state, and an opaque secret reference; migrate the existing single-provider fields into one newly generated UUID profile, set that UUID as primary, and leave `fallbackProfileIds` empty. Never use a literal `default` profile ID: persist the generated UUID on first migration and reuse it on every later load/migration. The migration is idempotent and must never replace a stored provider/profile ID merely because the registry does not currently recognize it.
- [ ] Keep secret resolution host-mediated inside `G:/AI/All lumiverse repos/Lumiverse/src/services/embeddings.service.ts`: resolve an enabled profile's opaque reference through `G:/AI/All lumiverse repos/Lumiverse/src/services/secrets.service.ts` immediately before the embedding driver invocation (preserving the existing provider-scoped legacy-secret migration), and return only `hasSecret`/redacted status from `embeddings.routes.ts`. Neither the provider RPC/DTO/result, route response, frontend state, worker callback context, WebSocket event, logs, errors, cache fingerprints, nor tests may expose a raw key or secret-reference value. On denied resolution, do not invoke the driver and return the typed redacted permission-denied/unavailable status. Select the driver only after profile resolution: first a live registered embedding driver, then the existing built-in provider path (including Vertex via `google-vertex`), otherwise classify the configured provider ID as `unavailable` without overwriting it.
- [ ] Store each embedding secret under `embedding-profile/<profileUuid>/<field>` (for example, `apiKey`), never under a provider-wide or literal-default key. Migrate each legacy secret into the generated profile namespace atomically with its persisted profile UUID, leave the legacy value available only until the migration commit succeeds, and clean it up only after the new secret is readable. Profile update/delete cleanup may remove only that profile's namespace; no operation may enumerate or delete another profile's secrets. Add migration, failed-migration rollback, cleanup, and cross-profile isolation cases proving generated UUID stability, no literal-default namespace, exact legacy-to-profile secret movement, and no cross-profile secret access or deletion.
- [ ] Implement ordered execution in `G:/AI/All lumiverse repos/Lumiverse/src/services/embeddings.service.ts`: primary profile, then de-duplicated enabled `fallbackProfileIds` in stored order. Skip a profile with a missing profile, disabled/missing secret, unavailable/unknown provider, incompatible declared/returned dimensions, invalid model response, or per-profile timeout; record a redacted attempt reason and continue. A profile that returns a dimension different from the vector collection/model fingerprint is invalid and cannot be used to write/query that collection. When the primary profile ID is absent, unavailable, or fails and no valid fallback remains, return the typed `embedding_provider_unavailable`/`embedding_fallback_exhausted` result; do not silently reset to a default provider, mutate persisted selection, or perform a cross-model vector operation. Propagate caller cancellation and use the existing linked timeout signal for each attempted driver.
- [ ] Change the frontend only after the backend save/load contract exists: `G:/AI/All lumiverse repos/Lumiverse/frontend/src/api/embeddings.ts` serializes/deserializes the redacted profile DTO and error/status union; then the actual `EmbeddingsSettings` component at `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/SettingsModal.tsx` renders primary and ordered fallback profile selectors, Vertex project/region controls, model/dimension validation, unavailable/timeout badges, and explicit retry/save feedback. It must preserve an unknown provider/profile as an unavailable selected value and block apply/test until corrected; it must not replace it with the default option during hydration, save, provider refresh, or error handling.
- [ ] Add this embedding test matrix before implementation is accepted: `G:/AI/All lumiverse repos/Lumiverse/src/services/embeddings.service.connection-profiles.test.ts` — `migrates legacy single-provider config into one stable generated UUID profile`, `does not use a literal default profile or secret namespace`, `moves a legacy secret only into embedding-profile UUID field namespace`, `keeps profile secret migration retry-safe and cleanup isolated`, `selects the primary profile on bootstrap`, `selects the next eligible fallback after primary failure`, `skips missing unavailable and unknown provider profiles without rewriting their IDs`, `rejects model dimension mismatch before a vector write`, `times out one driver then tries the next profile`, `resolves the opaque secret only at embedding driver invocation`, `caller aborts primary without starting fallback`, `caller abort leaves no partial cache or write and returns stable cancellation`, and `fails explicitly when no fallback remains`, then `G:/AI/All lumiverse repos/Lumiverse/src/routes/embeddings.routes.connection-profiles.test.ts` — `saves and loads ordered connection profiles without secret values`, `redacts profile secrets from config and failure responses`, and `returns redacted permission denied without invoking the embedding driver`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/api/embeddings.connection-profiles.test.ts` — `preserves unavailable provider IDs through load and save`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/modals/SettingsModal.embeddings-fallback.test.tsx` — `hydrates bootstrap profiles without resetting an unknown provider`, `saves primary and fallback order`, and `renders unavailable dimension and timeout states`. The caller-abort cases are blocking: the primary request must receive the caller `AbortSignal`, no fallback may start, no partial cache/vector write may remain, and cancellation must be a stable typed result. The secret-boundary cases must prove the host resolves a profile secret only immediately before driver invocation through `src/services/secrets.service.ts`, and that migration/update/delete cannot access or remove another profile's namespace; neither secret references nor values may enter SDK DTOs, provider RPC, frontend store, cache, logs, errors, or fixtures.

### Task 5.2: Memory Cortex secondary fallback

- [ ] Extend `G:/AI/All lumiverse repos/Lumiverse/src/services/memory-cortex/config.ts` (`memoryCortexConfig`) and its normalized defaults/migration with distinct `queryGeneration` and `memorySummarization` selections. Each selection has `primary` and optional `secondary` `{ connectionProfileId, model }` values plus the existing bounded retry/timeout policy; migrate the present `sidecar.connectionProfileId`/`sidecar.model` into both primary selections, preserve explicit `null` secondary values, and never substitute the active chat provider. Expose the redacted config/status DTO through `G:/AI/All lumiverse repos/Lumiverse/src/routes/memory-cortex.routes.ts` and `G:/AI/All lumiverse repos/Lumiverse/frontend/src/api/memory-cortex.ts`.
- [ ] Put orchestration in `G:/AI/All lumiverse repos/Lumiverse/src/services/memory-cortex/index.ts`, not in `sidecar-adapter.ts`: for each named purpose, execute primary with at most configured bounded retries/backoff, then secondary with the same bounded policy, then the configured deterministic heuristic or skip outcome. `sidecar-adapter.ts` remains a one-supplied-connection executor that accepts a signal and returns classified success/failure; it may not select profiles or initiate a fallback. Classify cancellation, timeout, transient transport/5xx/rate-limit, malformed LLM result/schema failure, and non-retryable configuration/auth failures so only eligible failures retry/fallback. Link each attempt to the caller signal and `sidecarTimeoutMs`; cancellation stops the chain rather than beginning a fallback.
- [ ] Commit sidecar-derived Cortex writes only once after the chosen result has passed validation; do not write per failed attempt, duplicate a chunk/consolidation summary during recovery, or mark a skipped chunk warm. Immediately before each `sidecar-adapter.ts` driver invocation, resolve its opaque connection secret through `src/services/secrets.service.ts`; never pass a secret/reference through provider RPC, state callback context, route/API DTO, result/error, cache, WebSocket event, or log. On denied resolution, do not call the sidecar and classify a redacted permission-denied/unavailable outcome. Route health diagnostics record only profile IDs and classified/redacted errors. Track per-purpose profile health/circuit state, open it after the configured consecutive transient failures, avoid an open profile until its cooldown, and reset its circuit on a successful probe/recovery so a recovered primary can be retried on the next operation.
- [ ] Implement the frontend after route/API changes: `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/settings/MemoryCortexSettings.tsx` displays separate query-generation and memory-summarization primary/secondary selectors and model pickers, retains unavailable configured IDs, and shows unavailable, timeout, malformed-result, circuit-open, and no-secondary outcomes without silently selecting another profile. Saving one purpose must not mutate the other's profile/model; refresh/hydration must preserve stored unavailable selections.
- [ ] Add this Cortex test matrix: `G:/AI/All lumiverse repos/Lumiverse/src/services/memory-cortex/config.secondary-fallback.test.ts` — `migrates legacy sidecar selection into both purpose primaries` and `preserves null and unavailable secondary selections`, then `G:/AI/All lumiverse repos/Lumiverse/src/services/memory-cortex/index.secondary-fallback.test.ts` — `uses secondary after a classified primary outage`, `cancels on timeout without duplicate writes`, `caller cancellation during primary stops the chain without a memory write`, `caller cancellation during retry stops the chain without a memory write`, `caller cancellation during secondary stops the chain without a memory write`, `does not invoke heuristic fallback after caller abort`, `rejects malformed primary output before secondary succeeds`, `resolves a sidecar secret only immediately before invocation through src/services/secrets.service.ts`, `does not expose sidecar secret references or values in SDK DTOs, provider RPC, frontend store, state callbacks, cache, logs, errors, or fixtures`, `returns redacted permission denied without invoking the sidecar`, `uses deterministic heuristic after both profiles fail`, `skips without writes when no secondary and skip is configured`, and `resets the primary circuit after recovery`, then `G:/AI/All lumiverse repos/Lumiverse/src/routes/memory-cortex.routes.secondary-fallback.test.ts` — `saves and loads separate redacted purpose selections`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/api/memory-cortex.secondary-fallback.test.ts` — `preserves unavailable selections and classified status`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/settings/MemoryCortexSettings.secondary-fallback.test.tsx` — `saves independent query and summary fallbacks` and `renders unavailable timeout and no-secondary states`. All caller-cancellation cases are blocking and must stop primary/retry/secondary transitions, perform no memory write, and never reach heuristic fallback after caller abort.

### Task 5.3: Edit and Send history behavior

- [ ] Add `src/db/migrations/103_edit_and_send_outbox.sql` and update the canonical `baseline.sql` with the same final schema. Add an integer non-null message `revision`/version column (with an explicit compatibility backfill and every edit incrementing it) for `expectedVersion` comparisons. Create `edit_and_send_requests` with `id`, `user_id`, `chat_id`, `request_id`, `request_fingerprint`, `branch_chat_id`, `edited_message_id`, nullable `target_message_id`/`target_swipe_index`, deterministic `generation_id`, response/cursor fields, timestamps, `UNIQUE (user_id, chat_id, request_id)`, and a fingerprint consistency check so a replay with a different body is rejected rather than reused. Create `generation_outbox` with `id`, `request_id` foreign key, `user_id`, `chat_id`, `branch_chat_id`, `edited_message_id`, nullable `target_message_id`/`target_swipe_index`, `expected_version`, deterministic `generation_id UNIQUE`, `mode` (`normal` or `swipe`), `status` constrained to `pending`, `claimed`, `running`, `completed`, `failed`, or `cancelled`, `lease_owner`, `lease_expires_at`, `attempt_count`, `next_attempt_at`, `last_error_code`, `terminal_reason`, `dispatched_at`, `completed_at`, `cancelled_at`, `created_at`, and `updated_at`. Index ready/expired claims by `(status, next_attempt_at, lease_expires_at, created_at)`, request lookup by `(user_id, chat_id, request_id)`, generation lookup by `generation_id`, and branch/reconciliation lookup by `(branch_chat_id, status, updated_at)`; retain terminal request/outbox rows for 30 days before batched cleanup, never before idempotency replay is no longer required.
- [ ] Add `POST /api/v1/chats/:chatId/edit-and-send` in `G:/AI/All lumiverse repos/Lumiverse/src/routes/chats.routes.ts` backed by `G:/AI/All lumiverse repos/Lumiverse/src/services/chats.service.ts`; no verified equivalent route exists. Ground the implementation in `chats.service.ts` around `3147` (`createChatBranchRows`) and `3260` (its transaction), `generate.service.ts` around `1698-1768` (`startGeneration`, generation IDs, and abort controllers), the in-memory queue/task IDs in `chat-pipeline-coordinator.service.ts`, and the existing frontend surfaces `frontend/src/api/chats.ts`, `MessageEditArea.tsx`, `useMessageCard.ts`, `useSwipeAction.ts`, and `InputArea.tsx`. Require `{ messageId, content, expectedVersion, requestId }`, authenticate/authorize the source chat and message ownership, reject empty content and a stale message revision with a conflict response, and persist/replay the matching `(userId, chatId, requestId, requestFingerprint)` result; the same request ID with a different fingerprint is a conflict. Return `{ branchChatId, editedMessageId, immediateAssistantId, generationCursor }` on the first and every matching idempotent replay; `immediateAssistantId` is the cloned branched assistant for swipe mode and `null` for normal mode, because the transaction must not create a normal assistant placeholder.
- [ ] Define the atomic conditional transaction: under the chat/message lock, authenticate ownership, compare `message.revision = expectedVersion`, and on success increment the edited message revision, create/reuse the branch, write the edited message, idempotency result, and exactly one `generation_outbox` row before commit. The branch operation clones only source history through the edit plus the immediate source assistant metadata as a swipe target; it never mutates source rows. A conditional no-op replay returns the persisted result, while a fingerprint mismatch or version mismatch cannot create any row. No in-memory enqueue, normal placeholder, or new swipe may occur in this transaction; only `startGeneration` owns creation of a normal assistant message or a new swipe.
- [ ] Add `src/services/edit-and-send-dispatcher.service.ts` as the sole outbox owner. Claim/reclaim inside `BEGIN IMMEDIATE`: select one eligible row ordered by `(next_attempt_at, created_at, id)` where status is pending/eligible or the claimed/running lease is expired, attempt count is below `MAX_ATTEMPTS`, and the lease is bounded; issue a guarded `UPDATE ... RETURNING` (or `UPDATE` followed by an immediate `changes() = 1` ownership check) that sets the generated lease owner, expiry, status, and incremented attempt count, then `COMMIT`. Roll back on failure and retry SQLite busy/locked errors with bounded backoff. Transition `claimed -> running -> completed|failed|cancelled` only with owner/lease guards. Use deterministic exponential backoff with a cap, `next_attempt_at`, redacted `last_error_code`, and terminal reason/timestamps; release the lease on shutdown. Startup recovery runs after migrations in `src/main.ts`, reclaims expired leases, marks exhausted rows terminal, reconciles orphaned branch/request/outbox rows, and schedules retry; cancellation checks ownership/status atomically, marks pending/claimed rows cancelled without generation, and aborts an owned running generation before settling it. Cleanup removes only expired terminal rows and unreferenced provisional artifacts, never a referenced branch/message/swipe or an immutable source row.
- [ ] Exercise `src/db/migrate.ts` against both a fresh database and a fixture/database snapshot from immediately before migration 103: verify `103_edit_and_send_outbox.sql` applies once, is idempotent on rerun, preserves existing messages and revisions, creates every constrained column/index, and leaves `baseline.sql` schema-equivalent. Run startup recovery only after the migration promise resolves; on shutdown call the dispatcher lease-release path and prove an interrupted worker leaves reclaimable rows rather than an owner-held lease.
- [ ] The dispatcher invokes `generate.service.ts` through the exact `startGeneration` handoff with additive `GenerateInput.generationId` and `dispatchId`; swipe mode also passes the branched existing target message ID, while normal mode passes neither a pre-created placeholder nor swipe target. `startGeneration` exclusively creates the normal assistant message or the new swipe, enforces unique `generationId` idempotency, persists and returns the target message ID and swipe index, and replays the same persisted target IDs for that generation. Persist those returned IDs to `generation_outbox` and the request result. If the process crashes after target staging but before this update, replaying the same `generationId` must return the same target and allow the dispatcher to finish the outbox update; partial-content cancellation must preserve only the generation's defined cancellation state and leave no duplicate target. The coordinator is invoked only after a durable claim and is never the source of truth.
- [ ] Define the exact branch algorithm: locate the immediate visible subsequent assistant in source history (not merely the latest assistant); create or reuse the branch, copy required history through the edited user message, and clone that immediate assistant's message and swipe metadata into the branch only as the existing swipe target, without mutating source rows. Dispatch swipe generation against that branched target exactly once; `startGeneration` creates exactly one new swipe. If no subsequent assistant exists, the transaction leaves no assistant placeholder and dispatches normal generation exactly once; `startGeneration` creates exactly one normal assistant message. Define exact source/branch message and swipe counts for each success, replay, retry, crash-after-staging, partial-cancellation, and duplicate-delivery path; auth denial, stale revision, replay, cancellation, recovery, and reconciliation must leave source history immutable and may not create a duplicate target or generation.
- [ ] In frontend order, first add the typed endpoint/client and response union to `G:/AI/All lumiverse repos/Lumiverse/frontend/src/api/chats.ts`; then wire `G:/AI/All lumiverse repos/Lumiverse/frontend/src/hooks/useMessageCard.ts` to submit the version/request ID and switch the active chat to returned `branchChatId`; finally add the accessible `Edit and Send` action to `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageEditArea.tsx` while retaining Save/Cancel. Reuse the existing swipe and generation controls in `G:/AI/All lumiverse repos/Lumiverse/frontend/src/hooks/useSwipeAction.ts` and `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.tsx` by attaching to `generationCursor`; neither may issue its normal send/request after `edit-and-send` has accepted. Expose pending/error/cancelled state, allow an explicit new-request-ID retry after a terminal failure, and cancel the returned cursor through the existing generation cancellation path.
- [ ] Add this blocking Edit-and-Send test matrix: `G:/AI/All lumiverse repos/Lumiverse/src/db/migrate.edit-and-send-outbox.test.ts` (through `src/db/migrate.ts`) — `migrates fresh and pre-103 databases to the same edit and send schema`, `reruns 103 without schema or data drift`, and `matches baseline schema`, then `G:/AI/All lumiverse repos/Lumiverse/src/routes/chats.routes.edit-and-send.test.ts` — `rejects unauthenticated or foreign chat edits`, `returns a stale revision conflict`, `replays the requestId response without a second generation`, `cancels before dispatch without invoking generation`, and `cancels after dispatch through the existing generation cancellation path`, then `G:/AI/All lumiverse repos/Lumiverse/src/services/chats.service.edit-and-send.test.ts` — `edits a tail user message without a precreated assistant`, `branches a historical edit without mutating later source history`, `locates the immediate visible assistant and clones its swipe metadata as the target`, `leaves no placeholder and uses normal generation when no assistant follows`, `commits branch edit idempotency result and generation outbox atomically`, `crash after commit before dispatch preserves the branch and cursor`, `dispatch failure after commit is recoverably failed or pending`, `restart recovers pending and expired leased dispatch rows`, `duplicate outbox claims deduplicate by requestId`, `retry delivery invokes generation once`, `cancels before dispatch without generation`, `cancels partial content after dispatch without a duplicate assistant`, `terminal reconciliation cleans only unreferenced provisional artifacts`, and `retries idempotently after a terminal generation failure`, then `G:/AI/All lumiverse repos/Lumiverse/src/services/edit-and-send-dispatcher.service.test.ts` — `claims and reclaims only rows with an eligible status and lease`, `rejects stale lease-owner transitions`, `releases leases on shutdown and recovers them after startup`, `backs off then exhausts retries`, and `reconciles only orphaned unreferenced artifacts`, then `G:/AI/All lumiverse repos/Lumiverse/src/services/chat-pipeline-coordinator.service.edit-and-send.test.ts` — `never enqueues before transaction commit` and `deduplicates post-commit execution`, then `G:/AI/All lumiverse repos/Lumiverse/src/services/generate.service.edit-and-send.test.ts` — `passes the branched swipe target exactly once`, `uses normal generation exactly once without a precreated placeholder`, `returns the same persisted target for generationId replay`, and `recovers crash after target staging before outbox update`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/api/chats.edit-and-send.test.ts` — `serializes expectedVersion requestId and returned generation cursor`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/hooks/useSwipeAction.edit-and-send.test.ts` — `honors the Edit and Send swipe threshold and uses the returned assistant target without a duplicate send`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/hooks/useMessageCard.edit-and-send.test.ts` — `switches to the returned branch before resuming generation`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/MessageEditArea.edit-and-send.test.tsx` — `renders the Edit and Send action label` and `activates Edit and Send by keyboard with accessible name`, then `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/chat/InputArea.edit-and-send.test.tsx` — `attaches to the edit-and-send generation cursor without submitting again`. These route/service/generation/outbox tests are blocking for invariant F and must prove exact source/branch message and swipe counts for normal, swipe, replay, retry, staged-target crash, and partial-cancellation paths; no duplicate generation; exact source immutability; auth/revision/replay/cancellation semantics; migration parity; startup/shutdown recovery; and terminal cleanup.

### Task 5.4: Settings and message integration

- [ ] Preserve existing SettingsModal dynamic tab registration and sidebar rendering as baseline.
- [ ] Add settings_tab, settings_section, settings_card_actions, message_actions, and message_edit_actions anchors where absent.
- [ ] Verify third-party settings panels and message actions use the public SDK, scoped roots, and idempotent cleanup.
- [ ] Run the Phase 5 test matrix in dependency order: `bun test src/services/embeddings.service.connection-profiles.test.ts src/routes/embeddings.routes.connection-profiles.test.ts src/services/memory-cortex/config.secondary-fallback.test.ts src/services/memory-cortex/index.secondary-fallback.test.ts src/routes/memory-cortex.routes.secondary-fallback.test.ts src/routes/chats.routes.edit-and-send.test.ts src/services/chats.service.edit-and-send.test.ts src/services/chat-pipeline-coordinator.service.edit-and-send.test.ts src/services/generate.service.edit-and-send.test.ts`, then `bun test frontend/src/api/embeddings.connection-profiles.test.ts frontend/src/components/modals/SettingsModal.embeddings-fallback.test.tsx frontend/src/api/memory-cortex.secondary-fallback.test.ts frontend/src/components/settings/MemoryCortexSettings.secondary-fallback.test.tsx frontend/src/api/chats.edit-and-send.test.ts frontend/src/hooks/useMessageCard.edit-and-send.test.ts frontend/src/hooks/useSwipeAction.edit-and-send.test.ts frontend/src/components/chat/MessageEditArea.edit-and-send.test.tsx frontend/src/components/chat/InputArea.edit-and-send.test.tsx`, then `npm --prefix frontend run typecheck` and `npm --prefix frontend run build:checked`. Record each named test and exit code in the existing evidence ledger; a failed caller-abort/no-fallback/no-write, secret-boundary, version/idempotency, commit-before-dispatch crash, post-commit dispatch failure, restart recovery, duplicate-claim, cancellation, terminal-reconciliation, source-immutability, exact-swipe/normal-generation, or duplicate-generation test blocks invariant F and the phase.

### Phase 5 gate

- [ ] F1 through F8 are green.
- [ ] Every profile migration, unavailable/unknown-provider, dimension/model mismatch, timeout, caller-abort/no-fallback/no-write, secret-resolution boundary, primary/secondary recovery, no-secondary, branch/in-place, stale-version, commit-before-dispatch crash boundary, post-commit dispatch failure, restart recovery, duplicate claim, cancellation before/after dispatch, terminal reconciliation, request-id replay, exact swipe/normal-generation selection, source immutability, swipe/a11y, and no-duplicate-generation branch above has automated evidence from the named matrix. Each is blocking for invariant F.

## Phase 6: Full Verification, Manual Acceptance, and Handoff

### Task 6.1: Automated command matrix

- [ ] Execute every Phase 0--5 matrix row below in its stated working directory before the full-suite commands. A matrix row passes only when its command exits 0, every named existing/new test passes, and its stated invariant/acceptance criterion is recorded with the raw count or artifact hash in `review/evidence-ledger.md`.
- [ ] Full SDK regression: working directory `G:/AI/All lumiverse repos/lumiverse-spindle-types`; run `npm run build`, `npm run build:consumer`, `npm pack --json`, then install the emitted `lumiverse-spindle-types-0.6.16.tgz` into the temporary packed-consumer fixture and run `npx tsc -p tsconfig.json --noEmit`. Tests are `test/package-exports-consumer.ts`, `test/legacy-0.6.15-consumer.ts`, and `test/packed-0.6.16-consumer.ts`; pass means emitted declarations resolve from `dist`, the legacy fixture remains source-compatible, and the packed tarball—not the SDK source tree—compiles. This proves A.
- [ ] Full suite regression: working directory `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite`; run `bun run typecheck`, `bun test`, and `bun run build`. The suite lifecycle case must prove no raw DOM scraping/private host contract remains after the promoted SDK path; pass means all three commands exit 0, proving A--C.
- [ ] Run the explicit raw-DOM enforcement command from `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite`: `bun test tests/suite-public-sdk-boundary.static.test.ts -t "rejects extension-owned raw MutationObserver and document-wide core-selector scraping"`. It must exit 0 and print `rawMutationObservers=0 documentWideCoreSelectorScrapes=0`; nonzero in either field blocks B/C. The host exposes `window.__spindleExtensionDiagnostics.getCounters(owner, generation)` only in test/e2e mode, returning `{ extensionOwnedRawMutationObservers, documentWideCoreSelectorScrapes, decoratorObservers, roots, callbacks, injectedNodes, registrations }`; every lifecycle test and e2e fixture asserts the first two fields are exactly `0`, rather than relying on source inspection alone.
- [ ] Add `G:/AI/All lumiverse repos/Lumiverse/frontend/src/components/icons/DynamicExtensionIcon.test.tsx` with named cases `renders every registered extension icon without an uncaught error` and `falls back for an unknown extension icon without a raw DOM lookup`. Run it in the Phase 4 regression command as `bun test frontend/src/components/chat/chatDockContract.test.ts frontend/src/lib/spindle/geometry-api.contract.test.ts frontend/src/components/quick-toolbar/QuickToolbar.geometry.test.tsx frontend/src/components/quick-toolbar/QuickToolbar.reorder.test.tsx frontend/src/components/icons/DynamicExtensionIcon.test.tsx`, and again in the Phase 6 host command below.
- [ ] Full host regression: working directory `G:/AI/All lumiverse repos/Lumiverse`; run `bun test`, `npm --prefix frontend run typecheck`, `bun test frontend/src frontend/src/components/icons/DynamicExtensionIcon.test.tsx`, and `npm --prefix frontend run build:checked`, then run the deterministic gzip comparison in Task 0.4. Pass means exit 0 with zero uncaught errors in fixture fallback/no-provider flows; it also requires the Phase 2 catalog assertion of exactly 58 canonical literals, the placement assertion of exactly 64 physical placements, `extensionOwnedRawMutationObservers=0`, `documentWideCoreSelectorScrapes=0`, and zero stale root/callback/observer/injected-node records after every named lifecycle path, proving B--F.

### Task 6.2: Manual acceptance flows

- [ ] Make browser acceptance runnable from the currently infrastructure-free repo: add the exact pinned frontend devDependency `@playwright/test: "1.56.1"` to `G:/AI/All lumiverse repos/Lumiverse/frontend/package.json`, record the identical exact resolution in `frontend/bun.lock` under Bun lockfile v1 policy, and add scripts `e2e:install: "bunx playwright install chromium"` and `e2e:spindle: "bunx playwright test e2e/seeded-extension-acceptance.spec.ts --config=playwright.config.ts --project=chromium-desktop --project=chromium-narrow"`. The browser binary install command is exactly `bunx playwright install chromium`; named CI workflow `.github/workflows/spindle-playwright.yml` must run it plus the platform browser dependencies install before the test command.
- [ ] Name `G:/AI/All lumiverse repos/Lumiverse/frontend/playwright.config.ts` as the sole Playwright config. All spec/config references are frontend-relative: the script is `e2e/seeded-extension-acceptance.spec.ts --config=playwright.config.ts`, and Playwright resolves them from `frontend`. It defines projects `chromium-desktop` and `chromium-narrow` with exact viewports `{ width: 1440, height: 900 }` and `{ width: 390, height: 844 }`, `baseURL: "http://127.0.0.1:4173"`, `retries: process.env.CI ? 2 : 0`, `timeout: 30_000`, `expect.timeout: 5_000`, `trace: "retain-on-failure"`, and `screenshot: "only-on-failure"`. Its backend `webServer` uses `cwd: "G:/AI/All lumiverse repos/Lumiverse"`, `command: "bun run src/index.ts"`, and config-provided `env: { SPINDLE_E2E: "1", PORT: "3100" }`; its frontend server uses `cwd: "G:/AI/All lumiverse repos/Lumiverse/frontend"`, `command: "bun run dev --host 127.0.0.1 --port 4173"`, and config-provided `env: { VITE_API_URL: "http://127.0.0.1:3100" }`. Use Windows-safe executable/argument configuration, `reuseExistingServer: !process.env.CI`, and bounded startup timeouts. The named CI workflow is `.github/workflows/spindle-playwright.yml` and must run install, browser dependencies, both projects, and failure artifact upload.
- [ ] Add `G:/AI/All lumiverse repos/Lumiverse/frontend/e2e/fixtures/seeded-extension-acceptance.fixture.ts` and `G:/AI/All lumiverse repos/Lumiverse/frontend/e2e/seeded-extension-acceptance.spec.ts`. The fixture owns test-only authenticated seed/cleanup through named backend endpoints or a fixture owner, requires `SPINDLE_E2E=1` and an isolated test database identifier, rejects non-test environments, seeds one extension generation plus provider/chat data for the current test owner, and tears down the generation, data, sessions, and database records in `finally` cleanup. Seed and diagnostics routes are test/e2e-only, require the authenticated test owner and environment guard, are absent from production builds/routes, and never expose production data. The spec captures `console` events with `type === "error"` and `pageerror` events into evidence arrays, exercises load/reload/unload/reload plus the seeded chat, and reads the diagnostics counter API `window.__spindleExtensionDiagnostics.getCounters(owner, generation)` after every transition.
- [ ] Require local and CI commands from `G:/AI/All lumiverse repos/Lumiverse`: `bun install --frozen-lockfile`, then `bun --cwd frontend install --frozen-lockfile`, then `bun --cwd frontend run e2e:install`, then `bun --cwd frontend run e2e:spindle`; CI additionally runs `bunx playwright install-deps chromium` after the Chromium install and publishes Playwright report/trace/screenshot output on failure. Acceptance is blocking only when both named projects run and exit 0, the console-error and pageerror arrays are empty, and every counter assertion is exact: `uncaughtErrors=0`, `extensionOwnedRawMutationObservers=0`, `documentWideCoreSelectorScrapes=0`, `decoratorObservers=1` per host runtime, exactly one registration for each of the 11 override owners in the live generation, and after unload `roots=0 callbacks=0 injectedNodes=0 registrations=0`. Record both project names, viewport JSON, counter JSON, console/pageerror arrays, commands, and exit codes in `review/evidence-ledger.md`; these are blocking B/C evidence.
- [ ] In the seeded chat, scroll repeated message/row/card mounts through virtualization, reuse a React node from scope A to B, detach a portal, then change V1/V2 toolbar geometry and icons, dock/undock, and press/click before 2,000 ms and after 2,000 ms. Pass means exactly 58 canonical spec literals and exactly 64 verified physical placements, no duplicate `(owner,generation,mount,scope)` root or top-dock anchor, no stale scope-A/portal resources, no drag activation before 2,000 ms, and at most one geometry/decorator update per animation frame batch; this proves B and D.
- [ ] Open ConnectionsPicker, switch/refresh/delete profiles, toggle Grid/List, search, then simulate a provider failure and stale refresh. Exercise embedding and Cortex primary/fallback/skip, then tail and historical Edit-and-Send. Pass means zero network calls on a provider-model cache hit, stale refresh data never overwrites current selection, zero uncaught errors in no-provider/fallback UI, and all recorded F-path results match the named tests; this proves C, E, and F.

### Task 6.3: Staging and implementation handoff

- [ ] **Boundary 1 -- pre-execution plan revision on `staging` (perform now):** synchronize the external artifact plan from this canonical file, then run the byte/hash parity check. Record `$expectedBase` and `$expectedPlanHead` in `review/evidence-ledger.md` before staging. Every native PowerShell command is its own line or is followed immediately by `if ($LASTEXITCODE -ne 0) { throw 'BLOCKING native command failed' }`; do not use semicolon command chains. Verify exact staging lineage and repository-local identity before amending only `$expectedPlanHead`; no later SDK, host, frontend, suite, release, or implementation action may amend this pre-execution plan-revision commit.
- [ ] For the plan-revision amendment only, run `git diff --check` on the allowed conductor paths and check `$LASTEXITCODE` immediately. The staging allowlist is `conductor/tracks/spindle_extensibility_ui_modernization_20260816/plan.md`, `conductor/tracks/spindle_extensibility_ui_modernization_20260816/review/evidence-ledger.md`, `conductor/tracks/spindle_extensibility_ui_modernization_20260816/review/baselines/**`, `scripts/perf/measure-frontend-entry-gzip.mjs`, `frontend/playwright.config.ts`, `frontend/e2e/**`, and `.github/workflows/spindle-playwright.yml`; stage no other path. Emit and record a structured staged-path result after `git diff --cached --name-only` with an immediate failure on an unexpected path. This check deliberately preserves unrelated pre-existing `frontend/dist`, diagnostics, generated-output, lockfile, and other worktree changes.
- [ ] Before and after this amendment, capture and compare the Lumiverse worktree with `$baseline = @(git status --porcelain); $baseline | ConvertTo-Json -Compress` (record that JSON in `review/evidence-ledger.md`); after the amendment run `git diff --cached --quiet; if ($LASTEXITCODE -ne 0) { throw 'BLOCKING staged paths remain after plan-revision commit' }; $after = @(git status --porcelain); $delta = @(Compare-Object $baseline $after); $delta | Format-Table -AutoSize; if ($delta.Count) { throw 'BLOCKING Lumiverse worktree differs from its recorded pre-execution baseline' }`. The baseline comparison, rather than a blanket empty-worktree requirement, is the clean-tree proof that preserves unrelated existing `dist` and diagnostics changes.
- [ ] **Boundary 2 -- SDK implementation and release commits:** perform SDK source, test, package/manifest, lockfile, build, release, and registry work only in `G:/AI/All lumiverse repos/lumiverse-spindle-types`, using new SDK implementation/release commits. Before each SDK commit, verify its expected base/head and staged paths with `$expectedBase = '<recorded SDK implementation base SHA>'; $expectedHead = '<recorded SDK HEAD before this commit>'; if ((git rev-parse HEAD).Trim() -ne $expectedHead -or -not (git merge-base --is-ancestor $expectedBase HEAD)) { throw 'BLOCKING unexpected SDK base/head' }; $allowed = '^(src/|test/|tests/|package\.json$|tsconfig[^/]*\.json$|bun\.lockb?$|package-lock\.json$|npm-shrinkwrap\.json$)'; $staged = @(git diff --cached --name-only); $unexpected = @($staged | Where-Object { $_ -notmatch $allowed -or $_ -match '(^|/)(dist|build|coverage|\.cache)(/|$)' }); [pscustomobject]@{ base = $expectedBase; head = $expectedHead; staged = $staged; unexpected = $unexpected } | ConvertTo-Json -Compress; if ($unexpected.Count -or -not $staged.Count) { throw 'BLOCKING SDK implementation staged-path allowlist failure' }`. SDK changes are expected during this boundary; do not require the SDK repository to have zero staged or working-tree changes while SDK implementation is in progress.
- [ ] **Boundary 3 -- Lumiverse implementation commits:** after the SDK release is available, create new implementation commits only in `G:/AI/All lumiverse repos/Lumiverse`; never amend the Boundary 1 plan-revision commit. Before each Lumiverse implementation commit, verify `$expectedBase = '<recorded Lumiverse implementation base SHA>'; $expectedHead = '<recorded Lumiverse HEAD before this commit>'; if ((git rev-parse HEAD).Trim() -ne $expectedHead -or -not (git merge-base --is-ancestor $expectedBase HEAD)) { throw 'BLOCKING unexpected Lumiverse base/head' }; $allowed = '^(src/|frontend/src/|frontend/e2e/|spindle-extensions/lumiverse_suite/(src/|test/|tests/|package\.json$|bun\.lockb?$|package-lock\.json$|npm-shrinkwrap\.json$)|test/|tests/|package\.json$|bun\.lockb?$|package-lock\.json$|npm-shrinkwrap\.json$|frontend/package\.json$|frontend/(bun\.lockb?|package-lock\.json|npm-shrinkwrap\.json)$)'; $staged = @(git diff --cached --name-only); $unexpected = @($staged | Where-Object { $_ -notmatch $allowed -or $_ -match '(^|/)(dist|build|coverage|\.cache)(/|$)' }); [pscustomobject]@{ base = $expectedBase; head = $expectedHead; staged = $staged; unexpected = $unexpected } | ConvertTo-Json -Compress; if ($unexpected.Count -or -not $staged.Count) { throw 'BLOCKING Lumiverse implementation staged-path allowlist failure' }`. The allowlist permits only implementation source, tests, manifests, and lockfiles; it never permits `frontend/dist` or other generated assets, and it preserves unrelated existing generated/diagnostics worktree changes by allowlist.
- [ ] **Boundary 4 -- final verification and clean-tree handoff after implementation:** after all implementation and release commits and Phase 6.1--6.2 acceptance evidence, run each native command separately in each repository and check `$LASTEXITCODE` immediately: `git diff --check`, `git diff --cached --check`, `git diff --cached --quiet`, `git status --porcelain`, `git rev-parse HEAD`, and `git branch --show-current`. Any nonzero result is blocking. Compare each final `git status --porcelain` output with its recorded pre-execution baseline and fail only for new or changed paths outside that baseline; this is the final clean-tree check for both `G:/AI/All lumiverse repos/Lumiverse` and `G:/AI/All lumiverse repos/lumiverse-spindle-types`. Record each repository's base/head, staged-path allowlist result, commit hashes, verification results, and baseline-aware clean-tree result in the ledger.

## Final Acceptance Mapping

| Acceptance criterion | Required matrix evidence and objective pass condition |
|---|---|
| A | Phase 0 SDK build and canonical `test/scripts/verify-packed-consumer.mjs` row, Phase 1 public-V2/host-contract row, the authorized post-Task-1.3 release row, and the Phase 6 SDK regression all pass. The final `0.6.16` package tarball is captured from `npm pack --json` by filename, resolved to an absolute path before use, and is the only artifact installed by consumer fixtures; public types remain additive, the 0.6.15 fixture remains source-compatible, and every package-root declaration resolves from the package `dist` output. No consumer manifest or production lock changes before the explicitly authorized release gate. |
| B | Phase 1/2 and seeded-browser rows prove exactly `58` canonical literals and literal records, exactly `64` physical placements through the Default renderer paths, and exactly `11` override hosts. Each override has exactly `1` live registration per owner/generation; each host runtime has exactly `1` decorator observer; duplicate roots for `(owner,generation,mount,scope)` equal `0`; and after ref detach, scope change, portal detach, unload, and reload, `roots=0 callbacks=0 injectedNodes=0 registrations=0`. Runtime and static counters are `extensionOwnedRawMutationObservers=0` and `documentWideCoreSelectorScrapes=0`. |
| C | Phase 3 migration, registry, RPC, consumer, suite, and browser rows prove migration `102` is idempotent; the SDK owns all six directional provider discriminators; the canonical key is `{effectiveScope, installationId, kind, id}` with monotonic `recordRevision`; and the host-derived scope model, two-stage no-secret broker, and recipient-scoped WebSocket events isolate every user/reconnect path. Permission denial, malformed/oversize payloads, timeout, cancellation, reload, unload, and no-provider flows have controlled results, `uncaughtErrors=0`, zero cross-user visibility, zero stale callbacks, and zero matching records/pending calls after revocation or unload. |
| D | Phase 4 toolbar rows and browser acceptance prove `quickToolbarPlacement` is only `'floating' | 'chat_top_dock'`, both V1 and V2 use the one ChatView dock, duplicate top-dock anchors equal `0`, settled unused main-axis space is `0` device pixels, drag activation before `2,000 ms` equals `0`, and geometry/decorator updates are at most `1` animation-frame batch. All `8` default actions appear exactly once in V1, V2, and the customizer. |
| E | The Phase 4.6 bridge/coordinator row and browser row prove legacy `connections.setActive` remains synchronous while `setActiveAcknowledged` and every enumerated caller await the one coordinator. Profile and reasoning state commit only after acknowledgement; stale acknowledgements and deleted-profile responses cannot win; cache-hit provider-model network calls equal `0`; Grid/List, search, persistence, reopen/reload, deletion fallback, and no-provider paths have `uncaughtErrors=0`. |
| F | Phase 5 and browser rows prove migration `103` parity/idempotency, immediate host-only secret resolution, and caller cancellation with zero fallback/write/partial-cache effects for Embeddings and Cortex. Edit and Send commits branch/idempotency/outbox state before dispatch; the dispatcher and startup recovery recover leases/cursors; `startGeneration` alone creates normal targets or swipes; normal paths have zero pre-created placeholders and swipe paths use the immediate visible assistant exactly once. Replay, crash, retry, cancellation, terminal reconciliation, and source immutability tests pass with duplicate generations equal `0` and fallback/no-provider `uncaughtErrors=0`. |

## Mandatory Executable Verification Matrix

All commands below are executed exactly in the stated working directory. “New” test files are required additions. Each row is a blocking gate: every command exits `0`, every named case passes, and the stated raw count, revision, hash, or release record is entered in `review/evidence-ledger.md`.

### Phase 0 — SDK compatibility/build

| Tasks | Working directory and exact command | Existing/new test file and exact case/pattern | Pass condition and proof |
|---|---|---|---|
| 0.2–0.3 | `G:/AI/All lumiverse repos/lumiverse-spindle-types`; run `npm ci`, check `$LASTEXITCODE`, then `npm run build`, check `$LASTEXITCODE`, then `npm run build:consumer`, check `$LASTEXITCODE`. | New `test/package-exports-consumer.ts` — package-root import of every promoted export; new `test/legacy-0.6.15-consumer.ts` — legacy-only public surface; new `test/packed-0.6.16-consumer.ts` — package-name-only packed consumer; `test/provider-runtime-union-parity.ts`, then `test/scripts/verify-packed-consumer.mjs` — canonical pack/install proof. | Exit `0`; exact pinned TypeScript from `package-lock.json` runs all named local fixtures through `tsconfig.consumer.json` with `noEmit`, then the canonical verifier proves the isolated packed tarball. No publication, consumer manifest, or production lockfile change occurs in Phase 0. Proves A. |
| 0.4 | Use disposable clean detached worktrees: measure the recorded base commit to create the explicit JSON baseline, then measure the recorded current implementation commit for comparison. Run each native build/measure command on its own line with immediate `$LASTEXITCODE` checks. | New `scripts/perf/measure-frontend-entry-gzip.mjs`; recorded baseline `review/baselines/frontend-entry-gzip.json`. | Baseline and comparison JSON identify their measured source commits/tree hashes and asset target deterministically. The main dirty `frontend/dist` is untouched; gzip is in-memory; delta is `<=5.00%`. |

### Phase 1 — promoted host bridge/loader lifecycle

| Tasks | Working directory and exact command | Existing/new test file and exact case/pattern | Pass condition and proof |
|---|---|---|---|
| 1.1–1.3 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test frontend/src/lib/spindle/loader.lifecycle.contract.test.ts frontend/src/lib/spindle/loader.lifecycle.test.ts frontend/src/lib/spindle/host-surface-registry.test.ts frontend/src/lib/spindle/geometry-api.contract.test.ts`, then `npm --prefix frontend run typecheck` | `loader.lifecycle.contract.test.ts` — `loader lifecycle contract cases pass in an isolated module graph`, then `loader.lifecycle.test.ts` — `frontend extension lifecycle cases pass in an isolated module graph`, then `host-surface-registry.test.ts` — `generation cleanup is precise and teardown is terminal`, then `geometry-api.contract.test.ts` — `type-checks the additive context surface`; Phase 0 package-root, legacy, packed, and provider-union fixtures. | Exit `0`; all V2 exports and SDK-owned provider unions compile additively; handles are idempotently disposed; a generation teardown is terminal; legacy context remains assignable. Proves A and lifecycle B. |
| 1 release gate | `G:/AI/All lumiverse repos/lumiverse-spindle-types`; after the preceding Phase 0/1 commands are green, run `npm whoami --registry https://registry.npmjs.org/`, then `npm owner ls lumiverse-spindle-types --registry https://registry.npmjs.org/`, then `npm view lumiverse-spindle-types@0.6.16 version --registry https://registry.npmjs.org/`, then `npm pack --json`; only with recorded explicit release authority, `npm publish --registry https://registry.npmjs.org/ --tag latest --provenance`; then verify registry version, tarball URL/integrity, `latest`, and provenance. | Phase 0 packed-consumer fixtures and Phase 1 public-V2/host-contract tests; release authority record and final package hash. | Publication is forbidden before Phase 1 evidence passes and forbidden without recorded explicit authority. The registry is exactly `https://registry.npmjs.org/`; the authenticated owner is authorized, `0.6.16` did not exist before publish, SDK identity/remote/tree checks pass, provenance is present unless the authority records an exception, and only then may the three consumers change manifests/production locks to `0.6.16`. |

### Phase 2 — all-58 mounts/11 overrides/decorator teardown

| Tasks | Working directory and exact command | Existing/new test file and exact case/pattern | Pass condition and proof |
|---|---|---|---|
| 2.1 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test frontend/src/lib/spindle/mount-points.test.ts frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx frontend/src/lib/spindle/dom-decorator-service.placements.test.tsx` | `mount-points.test.ts` — `publishes the authoritative catalog in exact order` and `asserts exactly 58 canonical mount literals`, then `dom-decorator-service.mounts.test.tsx` — one Default-renderer-path placement assertion per literal record; `dom-decorator-service.replay.test.tsx` — `replays repeated and virtualized mount anchors by stable key`, then `dom-decorator-service.placements.test.tsx` — `asserts exactly 64 physical mount placements`, the ten BubbleMessageDefault/MinimalMessageDefault assertions, and the SettingsModal/ProductivitySettings assertions. | Exit `0`; exactly `58` canonical literals/records and exactly `64` physical placements, each with an exact Default renderer path, JSX region, scope, and assertion. The counts are independently asserted. Proves B. |
| 2.2 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test frontend/src/lib/spindle/dom-decorator-service.mounts.test.tsx frontend/src/lib/spindle/dom-decorator-service.replay.test.tsx frontend/src/lib/spindle/dom-decorator-service.lifecycle.test.tsx frontend/src/lib/spindle/extension-root-stamp.test.ts` | New lifecycle cases — `registers and unregisters a ref anchor with required scope`, `moves a reused React node from scope A to scope B without stale root`, `keeps repeated concurrent rows isolated by live-anchor identity`, `recycles virtualized anchors without stale callbacks`, `removes a detached portal anchor before replay`, `removes generation resources on unload and creates fresh resources on reload`, `legacy observer fallback does not create duplicate root`, `shares one observer per host runtime`, `does not create duplicate root for generation mount scope`, `teardown removes all generation roots`, and `batches mount and teardown to one animation frame`. Existing `extension-root-stamp.test.ts` — `writes UUID ownership and identifier metadata without making metadata authoritative`. | Exit 0; every ref record contains mount/scope/live-anchor ID/owner/generation/node, scope is non-empty, observer count is 1/runtime, duplicate-root count is 0, and all root metadata/callback/observer/injected-node records are gone after each teardown path. Mount/teardown scheduling is <=1 animation frame per batch. Proves B security/performance threshold. |
| 2.3–2.4 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test frontend/src/lib/spindle/component-override-registry.test.tsx frontend/src/lib/spindle/settings-tab-bridge.test.ts frontend/src/lib/spindle/settings-bridge.test.ts` | New `component-override-registry.test.tsx` — one `'<Host> wraps replaces falls back and unloads'` case for each of the 11 table hosts, plus `rejects second override registration for owner and generation`. Existing settings bridge tests: all cases in `settings-tab-bridge.test.ts` and `settings-bridge.test.ts`. | Exit 0; exactly 11 hosts accept overrides, exactly one registration exists per owner/generation, fallback renders native UI with zero uncaught errors, and unload restores every host. Proves B. |
| 2.5 | `G:/AI/All lumiverse repos/Lumiverse`, then `npm --prefix frontend run typecheck`, then `npm --prefix frontend run build:checked`, then `bun test frontend/src/lib/spindle/dom-helper.scoped-style.test.ts frontend/src/lib/spindle/dom-decorator-service.lifecycle.test.tsx`; run the Phase 0.4 comparison command. | `dom-helper.scoped-style.test.ts`; lifecycle cases `rejects unsafe HTML/SVG and leaves no extension root after unload`, `clears root metadata callbacks observer entries and injected nodes on portal detach`, and `clears owner generation resources before reload`. | Exit `0`; unsafe sink bypasses=`0`; raw-observer and document-wide-core-selector counters=`0`; all lifecycle resource counters are `0` after every named teardown path; fallback uncaught errors=`0`; gzip delta is `<=5.00%`. Proves B. |

### Phase 3 — provider RPC/permissions/consumer refresh/unload

| Tasks | Working directory and exact command | Existing/new test file and exact case/pattern | Pass condition and proof |
|---|---|---|---|
| 3.1–3.2 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test src/db/migrate.provider-scope.test.ts src/spindle/provider-registry.test.ts src/spindle/runtime-transport.provider-rpc.test.ts src/spindle/worker-host.provider-rpc.test.ts src/spindle/provider-secret-resolution.test.ts` | New migration test — `applies 102_spindle_provider_scope once, reruns idempotently, and backfills scoped grants`; SDK `test/provider-runtime-union-parity.ts` — `keeps provider runtime discriminators directionally exhaustive`; provider cases include `allows same kind and id across different effective users`, `rejects duplicate kind and id within one installation scope`, `does not trust worker supplied user or owner`, `isolates list and invoke across users and shared operator installations`, `aborts timed-out and cancelled invocations`, `suppresses late provider_result after abort`, and the authenticated embedding/TTS/STT/sidecar two-stage broker, binary, stale-round, and redaction cases. | Exit `0`; migration `102` preserves decisions, is idempotent, and enforces the scoped key. All and only the six SDK union discriminators have the required direction; `recordRevision` is monotonic; scope/installation are host-derived; the broker leaks zero secrets/references/auth headers; each cancellation sends exactly `1` abort; and revoke/unload leaves matching records and pending calls=`0` with no cross-user effect. Proves C. |
| 3.3 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test src/routes/embeddings.routes.provider-registry.test.ts src/routes/memory-cortex.routes.provider-registry.test.ts frontend/src/ws/useWebSocket.provider-registry.test.ts frontend/src/components/settings/VoiceSettings.provider-registry.test.tsx frontend/src/api/embeddings.provider-registry.test.ts frontend/src/api/memory-cortex.provider-registry.test.ts frontend/src/api/voice-provider-registry.test.ts` | Cases `emits scoped provider_changed add remove and change after registry commit`, `projects provider_changed only into the authorized frontend scope`, `suppresses stale generation provider_changed events`, `resyncs provider projection before applying reconnect events`, `removes embedding TTS STT and sidecar options after unload without page reload`, `renders unavailable and timeout fallback`, `denied registration is not visible to consumers`, and `provider failure is isolated`. | Exit `0`; every committed event has one resolved authorized recipient, never an unscoped system broadcast; reconnect installs the authorized snapshot before later revisions; stale revision/generation events and all cross-user events applied=`0`; unavailable/revoked options are removed in place; uncaught errors=`0`. Proves C. |
| 3.4 | `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite`, then `bun install --frozen-lockfile`, then `bun pm ls lumiverse-spindle-types`, then `bun test tests/suite-public-sdk-boundary.static.test.ts tests/suite-public-sdk-consumer.test.ts tests/modules/lorebook-workspace-content-resize.test.ts tests/modules/lorebook-token-counts-lifecycle.test.ts tests/modules/portrait-dock-lifecycle.test.ts tests/modules/homepage-library-host-surface.test.ts tests/modules/lore-indicator-teardown.test.ts tests/modules/connections-picker-runtime.test.ts tests/modules/quick-toolbar-dom.test.ts tests/shared/theme.test.ts`, then `bun run typecheck`, then `bun run build`; then run the Task 3.4 packed-fixture compile using the absolute tarball path captured by the SDK verifier. | `suite-public-sdk-boundary.static.test.ts` — `rejects extension-owned raw MutationObserver and document-wide core-selector scraping`, then `suite-public-sdk-consumer.test.ts` — `uses only exported 0.6.16 SDK types`, `registers and unloads provider through public handles`, `mounts only in a host-provided scoped root`, and `unload disposes every registration surface and scoped root`; all Task 3.4 inventory tests. | Exit `0`; registry package version is exactly `0.6.16`; packed consumer compiles only against installed declarations; static and runtime raw-DOM counters are `0`; extension inventory removal conditions pass; post-unload roots/listeners/registrations=`0`. Proves A/B/C public-SDK boundary. |

### Phase 4 — toolbar/action catalog/picker persistence

| Tasks | Working directory and exact command | Existing/new test file and exact case/pattern | Pass condition and proof |
|---|---|---|---|
| 4.1–4.4 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test frontend/src/components/chat/chatDockContract.test.ts frontend/src/lib/spindle/geometry-api.contract.test.ts frontend/src/components/quick-toolbar/QuickToolbar.geometry.test.tsx frontend/src/components/quick-toolbar/QuickToolbar.reorder.test.tsx frontend/src/components/icons/DynamicExtensionIcon.test.tsx` | `geometry-api.contract.test.ts` — `normalizes quickToolbarPlacement migration and default`, then `QuickToolbar.geometry.test.tsx` — `renders exactly one top dock for V1 and V2 across floating and chat_top_dock placement`, `preserves dock placement through reload and viewport change`, `gives dock children correct order width and zero dead space`, `coalesces resize updates to one layout batch per frame`, and `cancels observer and animation frame work on unload`; reorder — `starts pointer drag only after 2000ms`; icon cases named in Task 6.1. | Exit `0` for every V1/V2 × floating/chat_top_dock × reload/viewport case: one top-dock anchor, one dock observer, zero stale dock height/dead space, at most one update per frame, no pre-`2,000 ms` drag, and zero residual observer/timer/listener/pointer state. Proves D. |
| 4.5 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test frontend/src/components/chat/chatDockerActionCatalog.test.ts frontend/src/lib/spindle/host-action-catalog.drift.test.ts` | Existing `host-action-catalog.drift.test.ts` — `every built-in catalog ref has a canonical host_action row`; new catalog cases named in the eight-row action table, plus `every default action appears exactly once in V1 V2 and the customizer`. | Exit 0; exactly eight default action IDs appear once per consumer, capability fallback is controlled, and unregister leaves no persisted hole. Proves D. |
| 4.6 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test frontend/src/lib/spindle/productivity-host-contracts.test.tsx frontend/src/lib/spindle/ctx-domain-api.test.ts frontend/src/store/slices/connections.active-profile.test.ts frontend/src/App.active-profile.test.tsx frontend/src/components/chat/InputArea.active-profile.test.tsx frontend/src/components/panels/ConnectionManager.active-profile.test.tsx frontend/src/components/connections-picker/ConnectionsPicker.persistence.test.tsx frontend/src/hooks/useAppInit.bootstrap-active-profile.test.ts src/services/bootstrap.service.active-profile.test.ts`, then `npm --prefix frontend run typecheck`, then `npm --prefix frontend run build:checked` | `productivity-host-contracts.test.tsx` — `renders ConnectionsPicker only through connections_picker.panel and launcher contract`, then `ctx-domain-api.test.ts` — `keeps legacy connections.setActive synchronous while setActiveAcknowledged awaits coordinator acknowledgement`; coordinator, App, InputArea, ConnectionManager, picker, and bootstrap named acknowledgement cases from Task 4.6. | Exit `0`, then `quickToolbarPlacement` enum is persisted/normalized; legacy bridge stays synchronous; acknowledged bridge plus every listed caller commits profile/reasoning atomically after acknowledgement; bypass writers=`0`; cache-hit calls=`0`; stale/deleted responses cannot win; no-provider uncaught errors=`0`. Proves D/E. |

### Phase 5 — embeddings/Cortex/Edit-and-Send

| Tasks | Working directory and exact command | Existing/new test file and exact case/pattern | Pass condition and proof |
|---|---|---|---|
| 5.1 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test src/services/embeddings.service.connection-profiles.test.ts src/routes/embeddings.routes.connection-profiles.test.ts frontend/src/api/embeddings.connection-profiles.test.ts frontend/src/components/modals/SettingsModal.embeddings-fallback.test.tsx` | New blocking cases include `caller aborts primary without starting fallback`, `caller abort leaves no partial cache or write and returns stable cancellation`, and `resolves the opaque secret only at embedding driver invocation`, in addition to the Task 5.1 migration/fallback/dimension/redaction cases. | Exit 0; caller `AbortSignal` cancels primary, starts no fallback, and leaves no cache/write; host-only immediate secret resolution exposes neither references nor values in SDK/provider DTOs, frontend/cache/log/error/fixture surfaces; fallback ordering and unavailable-provider results are controlled with zero uncaught errors. Proves F. |
| 5.2 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test src/services/memory-cortex/config.secondary-fallback.test.ts src/services/memory-cortex/index.secondary-fallback.test.ts src/routes/memory-cortex.routes.secondary-fallback.test.ts frontend/src/api/memory-cortex.secondary-fallback.test.ts frontend/src/components/settings/MemoryCortexSettings.secondary-fallback.test.tsx` | New blocking cases include `caller cancellation during primary stops the chain without a memory write`, `caller cancellation during retry stops the chain without a memory write`, `caller cancellation during secondary stops the chain without a memory write`, `does not invoke heuristic fallback after caller abort`, and the immediate `src/services/secrets.service.ts` resolution/no-escape case, plus the existing primary/secondary/heuristic/skip cases. | Exit 0; caller cancellation stops every Cortex transition, writes nothing, and cannot reach heuristic fallback; host-only secret resolution exposes neither references nor values; each fallback/skip state is rendered without uncaught error. Proves F. |
| 5.3–5.4 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun test src/db/migrate.edit-and-send-outbox.test.ts src/routes/chats.routes.edit-and-send.test.ts src/services/chats.service.edit-and-send.test.ts src/services/edit-and-send-dispatcher.service.test.ts src/services/chat-pipeline-coordinator.service.edit-and-send.test.ts src/services/generate.service.edit-and-send.test.ts frontend/src/api/chats.edit-and-send.test.ts frontend/src/hooks/useMessageCard.edit-and-send.test.ts frontend/src/hooks/useSwipeAction.edit-and-send.test.ts frontend/src/components/chat/MessageEditArea.edit-and-send.test.tsx frontend/src/components/chat/InputArea.edit-and-send.test.tsx`, then `npm --prefix frontend run typecheck`, then `npm --prefix frontend run build:checked` | Migration cases `migrates fresh and pre-103 databases to the same edit and send schema`, `reruns 103 without schema or data drift`, and `matches baseline schema`; dispatcher/startup, outbox claim, crash/retry/cancellation/reconciliation, branch, generation replay, target-selection, and accessible UI cases named in Task 5.3. | Exit `0`; migration `103` is schema-equivalent/idempotent; commit happens before dispatch; startup recovery and dispatcher leases recover exactly once; duplicate generation count=`0`; normal path pre-created placeholders=`0` and `startGeneration` creates exactly `1` normal target; immediate-assistant swipe path calls `startGeneration` once with that target and creates exactly `1` swipe; source history remains immutable; cancellation/reconciliation leave no duplicate or referenced-artifact deletion. Proves F. |

### Phase 6 — full acceptance and delivery

| Tasks | Working directory and exact command | Existing/new test file and exact case/pattern | Pass condition and proof |
|---|---|---|---|
| 6.1 | `G:/AI/All lumiverse repos/lumiverse-spindle-types`, then `npm run build`, then `npm run build:consumer`. `G:/AI/All lumiverse repos/Lumiverse/spindle-extensions/lumiverse_suite`, then `bun run typecheck`, then `bun test`, then `bun run build`, then `bun test tests/suite-public-sdk-boundary.static.test.ts -t "rejects extension-owned raw MutationObserver and document-wide core-selector scraping"`. `G:/AI/All lumiverse repos/Lumiverse`; run every Phase 0–5 row, then `bun test`, then `npm --prefix frontend run typecheck`, then `bun test frontend/src frontend/src/components/icons/DynamicExtensionIcon.test.tsx`, then `npm --prefix frontend run build:checked`; and the Phase 0.4 gzip comparison. | Aggregate every named matrix test; raw-DOM case prints `rawMutationObservers=0 documentWideCoreSelectorScrapes=0`; icon cases `renders every registered extension icon without an uncaught error` and `falls back for an unknown extension icon without a raw DOM lookup`. | Every command exits `0`; all A–F numeric thresholds hold; `uncaughtErrors=0`; raw-DOM counters=`0`; teardown counters=`0`; gzip delta=`<=5.00%`. |
| 6.2 | `G:/AI/All lumiverse repos/Lumiverse`, then `bun install --frozen-lockfile`, then `bun --cwd frontend install --frozen-lockfile`, then `bun --cwd frontend run e2e:install`, then `bun --cwd frontend run e2e:spindle`. In CI additionally run `bunx playwright install-deps chromium` after Chromium installation and publish Playwright report, trace, and screenshot outputs on failure. | `frontend/package.json` pins `@playwright/test` exactly to `1.56.1` with identical `frontend/bun.lock` resolution; scripts are exactly `e2e:install: "bunx playwright install chromium"` and `e2e:spindle: "bunx playwright test frontend/e2e/seeded-extension-acceptance.spec.ts --config=frontend/playwright.config.ts --project=chromium-desktop --project=chromium-narrow"`. `frontend/playwright.config.ts` is the sole config, with Chromium desktop `1440x900`, Chromium narrow `390x844`, backend/frontend web servers, retries/timeouts/trace/screenshot settings from Task 6.2. The authenticated, isolated `SPINDLE_E2E=1` fixture and seeded spec perform guarded seed/finally cleanup and collect `console.error`, `pageerror`, and diagnostics after each transition. | Both named projects exit `0`; browser diagnostics are `uncaughtErrors=0`, `extensionOwnedRawMutationObservers=0`, `documentWideCoreSelectorScrapes=0`, `decoratorObservers=1` per host runtime, each of `11` override owners has exactly `1` live registration, and after unload `roots=0 callbacks=0 injectedNodes=0 registrations=0`. The virtualization/scope/portal, dock/drag, picker/stale-refresh, provider-failure, Embeddings/Cortex, and Edit-and-Send scenarios meet their phase thresholds. |
| Pre-execution Boundary 1 and final Boundary 4 | Before any SDK or Lumiverse implementation commit, on `G:/AI/All lumiverse repos/Lumiverse` staging, run the Task 6.3 canonical/artifact SHA-256 parity command, the conductor-only `git diff --check` command, and its explicit staged-path allowlist check; record pre-execution base/head and worktree baseline, then amend only the plan-revision commit. After all implementation/release commits and acceptance rows, in both repositories run `git diff --check`, then `git diff --cached --check`, then `git diff --cached --quiet`, then `git status --porcelain`, then `git rev-parse HEAD`, then `git branch --show-current`. | Canonical plan/artifact hash record; recorded pre-execution base/head, commit identity, staged allowlist result, and baseline-aware final clean-tree record. | The plan-revision commit is a completed pre-execution boundary and is never amended by SDK or Lumiverse implementation commits. Canonical/artifact SHA-256 hashes match; no unexpected staged path exists at either boundary; final status differs from the recorded baseline by `0` new/changed paths; both repositories report their recorded heads and zero staged paths. |
